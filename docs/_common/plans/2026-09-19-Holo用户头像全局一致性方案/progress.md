# Progress: Holo 用户头像全局一致性方案

## 2026-09-19

- 已确认项目根目录存在另一项活跃规划，采用功能独立目录，避免覆盖。
- 已确认仓库有大量并行改动，本轮不修改产品代码。
- 已执行 Phase 1：现状盘点。
- 已定位 Apple 登录服务、设置页账号区、个人页固定头像和现成图片选择基础设施。
- 已确认对话用户头像为固定系统图标；Apple 登录没有头像字段；头像需要独立于 HoloProfile，但要与昵称保持统一资料体验。
- 已确认可复用图片处理、CloudKit 同步与账号删除基础设施；头像主数据需要单独的数据模型，不能只落本地文件或塞入昵称键值表。
- 已收敛数据方向：用户私有 iCloud 同步的小型单图 + 显式删除状态，不引入 Holo 后端图片上传。
- 已完成全局展示点分类：4 个真实身份位替换，导航/功能图标与 Holo AI 品牌头像保持不变。
- Phase 1 完成；已补齐权限文案、隐私政策、CloudKit Production schema 和测试基础等发布边界。
- 已完成 471 行完整开发计划，包含产品拍板、统一数据契约、精确文件映射、Phase 0–6、验收矩阵、风险和回退。
- 已复核当前 Xcode 工程确有 HoloTests 与 HoloUITests target；新测试仍需确认真实进入 target 并报告执行数。
- 本轮只新增本规划目录内 4 个 Markdown 文件，未修改产品代码、后端、生产环境或 Git 历史。
- 完成检查首次因非标准状态标记提示仍在进行；已改为 `[x]`，未重复使用错误格式。
- 第二次检查确认脚本实际要求 `### Phase` 与 `**Status:** complete`；已读取模板并按其结构修正。
- 用户追问圆形头像裁剪细节后复核：确认原“自动中心裁剪”不足，已将拖动/缩放裁剪、圆形蒙版、三尺寸预览和大图降采样提升为首版硬要求。
- 东林确认开始实施；实施范围为 iOS 代码、专项测试与本地/模拟器验证，Production CloudKit schema 部署继续保留为单独授权门。
- 完成实施基线核对：主 App 新文件自动编译；测试工程手动管理；已识别并保护 Core Data、设置页、个人页、Holo AI 消息气泡中的并行改动。
- 当前进入数据契约、图片处理和同步仓库实现；生产 CloudKit schema 部署仍保留为单独决策门。
- 已确认真实源码根目录为 `Holo/Holo APP/Holo/Holo/`；头像模型、服务和界面组件将全部新增在该同步目录内，避免工程文件改动。
- 已定位数据模型、仓库初始化和三个头像消费面的精确接入点；下一步开始新增代码文件。
- 已新增头像 Core Data 实体、同步仓库、裁剪几何、图片降采样/压缩处理器、统一头像组件与圆形裁剪器。
- 已把头像实体接入共享数据模型，并在 Core Data ready 后初始化头像仓库；尚未完成 UI 编辑入口和编译验证。
- 已确认设置页/个人页昵称入口、账号删除回调、Holo AI 用户气泡和现有相册/相机封装的局部替换范围。
- 已完成统一 `UserProfileEditorView`：支持相册、拍照、圆形拖动/缩放裁剪、三尺寸预览、更换、移除和昵称保存。
- 已接入设置账号卡、设置个人资料入口、个人页与 Holo AI 用户消息；Apple 登录只显示认证角标，头像本体始终使用同一份 Holo 用户头像。
- 已把账号删除后的内存头像同步清空，并将相机权限文案扩展为头像/任务附件共用描述。
- 已定位隐私政策需补充的四个条款位置，并确认专项几何测试可沿用现有 standalone 测试方式。
- 已更新应用内隐私政策：明确头像采集范围、用途、私人 iCloud 存储、不会发送给 AI/后端以及更换/移除权利。
- 已新增头像裁剪几何 standalone 测试，覆盖横图、竖图、缩放、拖动边界和缩放上下限。
- Phase 6–9 的首轮实现已完成，开始进入专项测试、编译错误修复与回归核查。
- 改动格式检查通过；头像裁剪几何 16 项 standalone 断言全部通过。
- 首次 Xcode 工程检查被当前沙箱的 Swift/Simulator 缓存权限阻断，属于环境问题而非编译结论；已改用项目既有授权前缀继续验证。
- 完整构建已进入实际编译阶段，首次失败发生在 `HoloWidgets` 编译共享 `CoreDataStack.swift`：新头像实体文件尚未加入小组件共享模型成员；主 App 的头像代码还未暴露其他编译结论。
- 已确认精确错误为小组件 target 找不到 `createUserAvatarEntity()`，并定位到其手动 Sources 与 Recovered References 配置，准备做最小工程文件补充。
- 已把头像实体描述与 NSManagedObject 类加入 HoloWidgets 的共享模型 Sources；第二轮编译继续推进后发现实体默认值不应依赖仅属于主 App 仓库的 `UserAvatarState` 类型。
- 已移除小组件共享模型对主 App 枚举的依赖，小组件侧已越过头像实体编译；主 App 首个错误收敛为头像仓库缺少显式 Combine 导入。
- 已补齐 Combine 依赖；增量构建正在继续，暂无新错误输出。
- 主 App 已完成头像组件及大部分业务文件编译，目前进入全工程后段编译/链接；输出均为仓库既有警告，尚无新的头像相关错误。
- 完整 Holo Debug / generic iOS Simulator 构建通过（exit 0），覆盖主 App、HoloWidgets、共享 Core Data 模型与新增头像文件。
- 已复核账号删除实现确实覆盖全部非抽象 Core Data 实体，头像不会成为删除遗漏项。
- 已补齐裁剪页保存中的遮罩、禁用重复点击和不可中途关闭，避免连续点击造成重复头像版本。
- 已确认 XCTest 可复用唯一内存模型；下一步增加图片输出与头像实体契约测试，并单独跑该测试类。
- 已新增并纳入 HoloTests 的 `UserAvatarFeatureTests`：验证模型默认值/二进制存储，以及真实图片输出尺寸、体积和 GPS 元数据剥离。
- 已选定现有 iPhone 模拟器，只运行该头像专项测试类；当前处于测试构建阶段。
- 头像专项 XCTest 首轮暴露测试文件缺少 Core Data 导入，已在测试侧修正，不涉及产品代码。
- `UserAvatarFeatureTests` 定向 XCTest 共执行 2 项且全部通过（exit 0），覆盖头像实体契约与真实图片 512×512 输出、体积上限、GPS 元数据剥离。
- 结果包复核为 `status = succeeded`、`testsCount = 2`；工程已有警告不属于本头像改动，本轮未扩大范围处理。
- 本轮实现与自动化验证完成；未执行 Production CloudKit schema 部署，也未把相册/相机授权、真机拍摄和双设备 iCloud 同步冒充为已验收。
