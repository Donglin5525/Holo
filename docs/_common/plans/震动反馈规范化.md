# 震动反馈规范化实现计划

## Context

用户希望在所有"提交"类操作中添加统一的震动反馈，提升操作确认感。目前项目已有部分实现，但代码分散且不统一。

**现状分析**：
- 习惯打卡、部分保存操作已有震动反馈
- 代码分散在各 View 中，每次都创建新的 Generator 实例
- 缺少统一的反馈规范

---

## 实现方案

### 1. 创建统一管理器

**新建文件**：`Holo/Holo APP/Holo/Holo/Utils/HapticManager.swift`

```swift
import UIKit

/// 触觉反馈管理器 - 统一管理 App 内所有震动反馈
enum HapticManager {

    // MARK: - Notification Feedback（通知反馈）

    /// 成功 - 用于保存/创建/提交成功
    static func success() {
        let generator = UINotificationFeedbackGenerator()
        generator.notificationOccurred(.success)
    }

    /// 警告 - 用于警告提示
    static func warning() {
        let generator = UINotificationFeedbackGenerator()
        generator.notificationOccurred(.warning)
    }

    /// 错误 - 用于操作失败
    static func error() {
        let generator = UINotificationFeedbackGenerator()
        generator.notificationOccurred(.error)
    }

    // MARK: - Impact Feedback（撞击反馈）

    /// 轻量 - 用于轻微交互（计数+1、选择切换）
    static func light() {
        UIImpactFeedbackGenerator(style: .light).impactOccurred()
    }

    /// 中等 - 用于中等交互（打卡、任务完成、长按触发）
    static func medium() {
        UIImpactFeedbackGenerator(style: .medium).impactOccurred()
    }

    /// 重度 - 用于重要操作
    static func heavy() {
        UIImpactFeedbackGenerator(style: .heavy).impactOccurred()
    }

    // MARK: - Selection Feedback（选择反馈）

    /// 选择变化 - 用于拖拽排序、滑动选择
    static func selection() {
        UISelectionFeedbackGenerator().selectionChanged()
    }
}
```

### 2. 反馈使用规范

| 反馈类型 | 方法 | 使用场景 |
|----------|------|----------|
| **成功** | `HapticManager.success()` | 保存账单、创建任务、创建习惯 |
| **中等撞击** | `HapticManager.medium()` | 习惯打卡、任务完成、长按触发 |
| **轻量撞击** | `HapticManager.light()` | 计数类习惯+1、数值微调 |
| **选择** | `HapticManager.selection()` | 拖拽排序、滑动选择 |

### 3. 需要修改的文件

#### 3.1 添加反馈（当前缺少）

| 文件 | 操作 | 添加代码 |
|------|------|----------|
| `AddTransactionSheet.swift` | 保存账单成功 | `HapticManager.success()` |
| `AddTransactionView.swift` | 保存账单成功 | `HapticManager.success()` |
| `AddTaskSheet.swift` | 保存任务成功 | `HapticManager.success()` |
| `TaskListView.swift` | 任务完成切换 | `HapticManager.medium()` |

#### 3.2 迁移到 HapticManager（当前有实现）

| 文件 | 当前实现 | 迁移到 |
|------|----------|--------|
| `HabitCardView.swift` | 直接调用 `UIImpactFeedbackGenerator` | `HapticManager.medium()` / `.light()` / `.success()` |
| `AddHabitSheet.swift` | 直接调用 `UINotificationFeedbackGenerator` | `HapticManager.success()` |
| `HomeView.swift` | 直接调用 Generator | `HapticManager.medium()` / `.selection()` |
| `DayCellView.swift` | 直接调用 Generator | `HapticManager.medium()` |
| `DarkModeManager.swift` | 直接调用 Generator | `HapticManager.light()` |

---

## 实现步骤

1. **创建 HapticManager.swift** - 统一管理器
2. **迁移现有代码** - 替换分散的 Generator 调用
3. **添加缺失反馈** - 为保存操作添加成功反馈
4. **验证测试** - 真机测试各场景震动效果

---

## 验证方法

1. 编译通过无警告
2. 真机测试以下场景：
   - [ ] 保存账单 → 成功震动
   - [ ] 创建任务 → 成功震动
   - [ ] 习惯打卡 → 中等震动
   - [ ] 任务完成切换 → 中等震动
   - [ ] 拖拽排序 → 选择震动
   - [ ] 计数类习惯+1 → 轻量震动

---

## 注意事项

- 震动反馈仅真机有效，模拟器无反馈
- 不要过度使用，避免用户疲劳
- 失败操作使用 `HapticManager.error()` 而非静默
