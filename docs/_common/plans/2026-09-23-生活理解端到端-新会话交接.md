# 「Holo 生活理解与主动筹备」新会话交接书

> 交接时间：2026-09-23 晚。上一会话已跑完 R0→R4 全部开发 + 后端发版 + QA 模拟器端到端。
> 当前唯一未闭环：**个性化候选没有进入规划生成**（已定位到检索层，30 分钟级排查量）。
> 新会话从本文 §5「下一步」直接开始，不需要重读全部历史。

## 1. 项目与文档指针

- 唯一实施依据：`docs/_common/plans/2026-09-23-Holo生活理解与Matter主动筹备-审查修订执行版.md`（V3）
- 阶段报告：同目录 `2026-09-23-R0基线与夹具交付报告.md`、`2026-09-23-R1-R4交付与真机验收准备报告.md`、`2026-09-23-真机验收链路.md`
- 目标：用户说「我去旅行」，Holo 从财务/任务/习惯/想法四域记录推断未说出口的责任（如宠物照护），进方案卡；一次「开始推进」建 Matter。

## 2. 已完成（全部测试绿，代码未提交）

- **R0** 夹具冻结 + 红测 + 62 组评测集（`HoloTests/.../Fixtures/`，heldout-20260923/ 已冻结）
- **R1** 四域观察层：`HoloLifeSourceObservation.swift`（finance/task/habit 三域分页+修订+软删哨兵+四域轮转 runPass）+ Snapshot 五字段
- **R2** Validator authorship 门禁（thirdPartyOnlyBasis）+ Reconciler 血缘去重（A07）+ 萃取 prompt v2（跨源聚合/泛称待消歧/代购与他人经历）
- **R3** 三红修复全转绿：bigram 词法（CJK≥2 重叠+条件匹配排除 goalSummary）、链接受控一跳、recurring 日期比对+isActive 只判结束+冻结区间（frame.resolvedInterval）；frame 七字段 + effect 八字段；R6 真实修订目录（sourceRevisionsProvider 注入）；MatterPlanLaunchCard「因你的情况」区块
- **R4** `HoloLifePlanRequirement.swift`：六维覆盖计算（A09-A12/A14）+ 效果映射 + 增量提案幂等（A19）
- **后端已发版两次**（生产验证过）：① prompt v1→v2（`personal_context_extraction`，PROMPT_VERSIONS bump）② prompt 空分支诊断日志。release/status 的 commit：`5c65a0be6`
- **测试**：standalone 12/13 绿（唯一红 ExtractorOrchestrator=并行会话「五路决策」未收口项，非本批）；XCTest 全绿（6 观察 + 11 桥接）；模拟器五大入口走查过闸门

## 3. QA 端到端已验证（模拟器真实跑，证据在 /tmp/lu-eval/ 截图 + 系统日志）

种子四域落库 ✓（任务上今日/打卡×2/记账待确认卡-¥128宠物分类/想法）→ 四域萃取真实轮转（EXTRACT-DIAG，候选 created）→ 规划闸开 → 云任务创建 → 云端生成成功（2120 字符）→ 方案卡「国庆东京大阪首次行前准备」7 项 + 开始推进 ✓ → 红线零违反 ✓ → 失败兜底卡正确 ✓。

自动化资产（可重复跑）：
- `HoloUITests/LifeUnderstandingJourneyUITests.swift`：`testLifeUnderstandingJourney`（全旅程）/ `testAskOnlyQ0`（轻量发问）
- 跑法：worktree `/tmp/holo-lu-work2`（见 §4），`xcodebuild test -project Holo.xcodeproj -scheme Holo -destination 'platform=iOS Simulator,id=F9F5EDDE-EB22-4951-A876-532BC5C53687' -derivedDataPath /tmp/holo-lu-dd2 -only-testing:HoloUITests/LifeUnderstandingJourneyUITests/testAskOnlyQ0`

## 4. 环境状态（重要，全部踩过坑）

| 项 | 状态 |
|---|---|
| 模拟器 | iPhone 17 `F9F5EDDE-EB22-4951-A876-532BC5C53687`（保持 booted，种子与候选在库） |
| **构建位置** | **`/tmp/holo-lu-work2`（worktree，基于 76d2d3a8a + 全部在途文件镜像）**——主仓当前被并行会话的 ChatMainColumn.swift 半成品挡编译；且 **HEAD（d669f802d 及之前）的 pbxproj 含历史损坏（DD17...A102 BuildFile ID 重复定义两次），检出即 damaged**，主仓工作区版是好的（并行会话删了重复行，未提交） |
| 模拟器 App 开关 | 四个 kill 键 + 两个记忆 v2 键已通过 **plutil 直写沙盒 plist**（`~/Library/.../Application/<容器>/Library/Preferences/com.holo.Holo.plist`）置开；注意 `simctl spawn defaults write` 写的是 shell 域**到不了 App**（假闭环坑）；改 plist 后需重启模拟器让 cfprefsd 冷读 |
| 后端 | 生产运行诊断日志版；**测试设备额度豁免已加**（`subscription_acceptance_overrides` 表 device `4FCD7B65-870A-42D2-B757-F448ACB46A4B` plus 至 2027-09-30）——**额度耗尽会伪装成云任务无日志秒败**（quota 分支无 log），鉴别法：解密 failure_reason（服务器 node + `HOLO_CLOUD_ANALYSIS_ENCRYPTION_KEY`） |
| idb | iOS 26 模拟器触摸全失效，交互只能 XCUITest |

## 5. 下一步（新会话从这里开始）

**唯一缺口：宠物候选没进生成 prompt。** 现象：最新云任务 `promptChars=344`（基线长度，候选=0；曾有 730 的任务=候选进过）。候选库里确有记录（萃取 created≥2）。

两个嫌疑（按性价比排序）：
1. **候选准入档位**：五路决策（并行会话在途接入 `HoloMemoryDecisionPolicy`）对新候选的 useLevel 判罚——若非 factEligible/qualifiedAdvice，`HoloContextAccessPolicy.selectAdviceCandidates` 会排除。**一步鉴别**：模拟器 App → 个人页 → AI 记忆实验室 → 看「情境库条目」列表中宠物相关候选的展示/状态；或读 DB（候选存 HoloMemoryRecord，走 CoreData 工具不方便就看实验室 UI）。
2. **检索信号未命中**：候选在库且可用，但 retrieve 没选中（bigram 修复后词面命中门槛高了，宠物候选 statement 与 Q0 词面几乎无重叠，靠语义向量——**embeddings 是否真的建了**：HoloContextEmbeddingStore 依赖 embedding provider，查模拟器日志 embeddings 请求与缓存状态）。

排查入口文件：`HoloContextPlanningCoordinator.preparePlanPrompt`（打 selected 数量日志最快）→ `HoloContextAccessPolicy.selectAdviceCandidates`（打 excluded 原因分布——excluded 字典现成，加一行 log 就能分辨嫌疑 1/2）。

**修好后**：重跑 `testAskOnlyQ0` → 预期方案卡出现「确认 10 月 1—7 日宠物照护」类项 + 「因你的情况」区块（后者依赖模型输出 planEffects，prompt v2 已教，可能需再调）→ 然后按《真机验收链路》请东林验收 → 代码按批提交（文件清单在 R1-R4 报告 §1）。

## 6. 本会话新增坑（已进 memory 的不再列）

- 云任务「无日志秒败」≈ quota 拒绝（无 log 分支），先用服务器密钥解密 failure_reason 再排查
- `simctl spawn defaults write` 自己写自己读=假闭环；写 App 沙盒用 plutil 直写 plist + 重启模拟器
- 仓库 HEAD 的 pbxproj 损坏（DD17...A102 重复）未提交修复；worktree 检出旧 commit 会踩；主仓工作区版健康
- 主仓 `Holo/Holo APP/Holo` 目标下 App 源码是**文件夹自动同步**（新 App 源文件零挂载），只有 HoloTests/HoloUITests 要手动挂 pbxproj 四处
- ExtractorOrchestrator standalone 断言失败=并行「五路决策」未收口（discard 不落库致「创建一条记录（实际 0）」），移交中
