# Holo 记忆长廊日历视图（周历 + 月历）方案

> 日期：2026-07-05
> 模块：记忆长廊（MemoryGallery）
> 状态：设计稿 v2 已确认，待东林 review → 转 writing-plans
> Mockup：`docs/superpowers/mockups/2026-07-01-calendar-mockup-v2.html`

---

## 一、背景与目标

生活星图（`MemoryConstellationCard`）作为记忆画廊「洞察 Tab」首卡，实际使用价值低。用 **周历 + 月历** 替换它，把记账/习惯/待办/想法聚合到一个统一日历视图，像市面日程软件平铺，让用户系统看到每天/最近做了什么。

**核心诉求**
- 一个视图看清每天做了什么
- 多模块数据（想法/习惯/待办/记账）平铺到时间维度
- 像日程软件一样可读、信息密集

**非目标**
- 不做新建/编辑入口（日历是只读回看）
- 不做 AI 洞察生成（AI 洞察保留为下钻补充）

---

## 二、现状结论（代码探索）

### 2.1 生活星图
- 位置：`MemoryGalleryView` 洞察 Tab 首卡，非独立页面
- 实现：SwiftUI `Canvas` 绘制 5 模块（习惯/财务/任务/思考/健康）星座连线
- 数据：寄生 `MemoryGalleryViewModel`，不独立取数（4 模块从 `MemoryItem` 聚合 + 健康从 `HealthRepository`）
- 文件：`MemoryConstellationCard.swift`(365) + `MemoryConstellationModels.swift`(163) + ViewModel 内 ~290 行

### 2.2 各模块日历聚合就绪度

| 模块 | 聚合字段 | day 维度现成查询 | hour 维度 |
|------|---------|----------------|----------|
| 记账 `Transaction` | `date`（非空） | `getTransactionsForDay` / `getDailySummaries` ✓ | 无现成，字段支持 |
| 习惯 `HabitRecord` | `date`（非空，可多次/天） | `getRecords(in:)`（按单习惯）✓ | 无现成，字段支持 |
| 待办 `TodoTask` | `completedAt` / `dueDate` / `plannedDate`（皆可选） | `getCompletionTrend` ✓ | 无现成 |
| 想法 `Thought` | `createdAt`（非空） | `getThoughtCountByDay` ✓ | 无现成，字段支持 |
| 观点 `Topic` | 无日历语义 | 无 | 不直接聚合 |

**关键**：所有时间字段带时分秒，hour 维度需新写 predicate，**不改 schema**。

---

## 三、设计决策（已与东林通过 mockup 确认）

1. **定位**：日历成为记忆画廊主线。移除生活星图，画廊顶部 Tab 改为 `周历 / 月历`（替代原 `洞察 / 明细`）。AI 洞察、热力图、时间线下沉为「选中日期详情」的下钻补充。
2. **周历**：默认 **列表视图**（7 天 × 横向事件流，信息全、字清晰，12px），保留 **网格视图**（7 列 × 24h 时间轴）作为切换。顶部「网格 / 列表」切换。
3. **月历**：31 天热力色块（复用 `MemoryHeatmapView` 5 档暖色阶 `#F5F2ED→#FF8C66`）+ 底部 4px 模块色条；选中当天下方平铺全部事件，按模块分组。
4. **模块配色**：
   - 记账 橙 `#F46D38`
   - 习惯 绿 `#22C55E`
   - 待办 蓝紫 `#6366F1`（**改动**：项目里待办原 = 橙，日历分色与记账撞，改蓝紫，取自记账分类色「购物」）
   - 想法 粉 `#EC4899`
   - 健康 蓝 `#3B82F6`（P3 月历可选图层）
5. **待办时间维度**：默认 `completedAt`（完成时间）；P2 支持切换 `dueDate` / `plannedDate`。
6. **观点 Topic**：不直接进日历。经其关联的 `Thought.createdAt` 间接体现。
7. **健康数据**：HealthKit 步数/睡眠作为月历可选图层（P3），不混进 5 模块色条。

---

## 四、架构

### 4.1 新增类型

**`CalendarEvent`（统一事件模型）**
```swift
struct CalendarEvent: Identifiable {
    let id: UUID
    let module: CalendarModule        // .finance/.habit/.todo/.thought/.health
    let date: Date                    // 带时刻，定位锚点
    let title: String                 // 列表/详情主标题
    let detail: String?               // 金额/数值/摘要
    let originID: NSManagedObjectID   // 跳转详情用（跨线程安全）
}

enum CalendarModule: CaseIterable {
    case finance, habit, todo, thought, health
    var color: Color { ... }          // 上述 5 色
    var iconName: String { ... }      // SF Symbol（新增前先验证存在）
}
```

**`CalendarEventProvider`（统一聚合层）**
- 职责：按区间从 4 个 Repository fetch，归一化为 `[CalendarEvent]`
- 无状态，由 View `.task` 驱动取数（`init()` 零 I/O）
- 各模块单独 do-catch，失败记 `Logger`、返回空数组，不阻塞其他模块
- hour 定位：直接用 `event.date` 时刻

```swift
struct CalendarEventProvider {
    func fetchEvents(in range: ClosedRange<Date>) async -> [CalendarEvent]
    func fetchDaySummary(_ date: Date) async -> [CalendarEvent]   // 月历当天详情
}
```

### 4.2 视图组件树

```
Views/MemoryGallery/Calendar/
├── CalendarRootView.swift            // 顶部周/月 Tab + ◀标题▶ 导航 + 今天
├── Weekly/
│   ├── WeeklyCalendarView.swift      // 网格/列表切换容器
│   ├── WeeklyListView.swift          // 列表视图（默认）
│   ├── WeeklyGridView.swift          // 24h 网格视图
│   └── WeeklyEventChip.swift         // 单事件块（列表/网格复用）
├── Monthly/
│   ├── MonthlyCalendarView.swift     // 31 格色块 + 翻页
│   ├── MonthCell.swift               // 单格（色深 + 底部色条）
│   └── DayDetailCard.swift           // 选中当天平铺（按模块分组）
└── CalendarFilterBar.swift           // 模块筛选（P2）
```

### 4.3 数据流

```
CalendarRootView
  └─ .task → CalendarEventProvider.fetchEvents(当前周/月区间)
       ├─ FinanceRepository.getTransactions(from:to:)
       ├─ HabitRepository.fetchAllRecords(in:)        ← 新增（不带 habitId 过滤）
       ├─ TodoRepository fetch(completedAt in range)  ← 复用 getCompletionTrend 内部逻辑
       └─ ThoughtRepository.fetchAll(range)           ← 复用现有区间 fetch
     → 归一化 [CalendarEvent] → 按 date 排序
     → WeeklyListView / MonthlyCalendarView 渲染

月历点格 → fetchDaySummary(date) → DayDetailCard 按模块分组
周历点事件块 / 月历点事件行 → 轻量事件详情弹层（新建；CalendarEvent 跨 4 模块，与现有 MemoryDetailView 绑定的 MemoryItem 不直接适配）
```

---

## 五、复用现有视觉语言

| 复用对象 | 用途 |
|---------|------|
| `MemoryHeatmapView` 5 档色阶 | 月历色深 |
| `HabitStatsMonthSwitcher` | `◀ 标题 ▶` 翻页器 |
| `DateRangeCalendar` | 月历格子布局参考（周一首、7×N、aspectRatio 1） |
| `DailySummaryNode` pill 样式 | 事件块：模块色浅底 + 左 3px 色条 |
| `HoloRadius.lg(16)` / `HoloSpacing.md(16)` | 卡片圆角与内边距 |
| `NumberFormatter.compactCurrency` | 金额紧凑显示（万/亿） |

---

## 六、涉及文件

**新增**（约 8 文件）
- `Models/Calendar/CalendarEvent.swift`、`CalendarModule.swift`
- `Services/Calendar/CalendarEventProvider.swift`
- `Views/MemoryGallery/Calendar/`（上述组件树）

**修改**
- `Views/MemoryGallery/MemoryGalleryView.swift`：Tab 改 `周历 / 月历`，移除生活星图卡渲染，AI 洞察/时间线下沉为下钻
- `Views/MemoryGallery/MemoryGalleryViewModel.swift`：移除 `constellation*` 计算属性（约 290 行）
- `Models/HabitRepository.swift`（或 `+Stats.swift`）：新增 `fetchAllRecords(in:)`（不带 `habitId` 过滤）
- 入口（`HomeView` 等）：确认记忆画廊入口不变

**删除**
- `Components/MemoryConstellationCard.swift`
- `Models/MemoryConstellationModels.swift`
- `HoloTests/.../MemoryGalleryViewModelConstellationTests.swift`
- `HoloTests/.../MemoryGalleryConstellationHealthTests.swift`
- `HoloTests/.../MemoryConstellationModelsTests.swift`

---

## 七、错误处理

- `CalendarEventProvider` 每个模块调用单独 do-catch，失败记 `Logger`（含模块名 + 区间），返回空数组，不阻塞其他模块
- UI 层：空区间显示「这段时间没有记录」友好空态，不展示错误
- hour 定位用 `event.date`；理论上非空时间字段不会有「无时刻」情况，兜底放当天 `00:00`

---

## 八、测试策略

**`CalendarEventProvider` 单测（核心逻辑，TDD）**
- 各模块区间聚合正确
- 多模块混合后按 `date` 排序
- 单模块失败不阻塞其他（mock Repository throw）
- 空区间返回 `[]`
- 跨天/跨周/跨月边界

**月历色块（纯函数）**
- 事件数 → 5 档色阶映射（复用 `MemoryHeatmapView` 阈值 0-1/2-3/4-5/6-9/10+）
- 当天模块集合 → 底部色条分段

**周历布局**
- 列表视图按天分组、天内按时间排序
- 网格视图事件块 top 定位（hour 计算）

> 测试改动必须用 `test_sim` 验证编译（`build_sim` 不含测试 target）。

---

## 九、分期实施

**P1（核心，可独立交付）**
- `CalendarEvent` + `CalendarModule` 模型
- `CalendarEventProvider`（4 模块：记账/习惯/待办/想法）
- 周历列表视图（默认）
- 月历色块 + 当天详情
- 移除生活星图、画廊 Tab 重组

**P2（增强）**
- 周历网格视图（24h 时间轴）
- 待办时间维度切换（completed / due / planned）
- 模块筛选栏
- 月历色块形式切换（热力 / 色环 / 徽章）

**P3（扩展）**
- HealthKit 健康图层（步数/睡眠，月历可选）
- 观点经想法间接体现的专门呈现

---

## 十、风险与边界

1. **Repository `init()` 零 I/O**：`CalendarEventProvider` 不在 init 取数，由 View `.task` 驱动（CLAUDE.md 约定）。
2. **hour predicate 性能**：大区间（整月）跨 4 模块 fetch 需关注。若慢，给 `Transaction.date` / `HabitRecord.date` / `Thought.createdAt` 加 `isIndexed`（`CoreDataStack+*Entities.swift`），不改 schema。
3. **生活星图移除的连带**：移除前确认 `MemoryGalleryViewModel` 的 AI 洞察、热力图、时间线、`featuredStories` 不依赖 `constellation*` 属性；`constellationSnippets` 仅被 `MemoryConstellationCard` 消费，安全删。
4. **数据精度**：事后补录的记账/想法时刻可能不准 —— 列表视图显式展示时间戳让用户自知；网格视图按时刻定位、空时段留白不强填。
5. **不改 schema**：所有改动基于现有 `date` / `createdAt` / `completedAt` 字段，无 Core Data 迁移。
6. **遵循编码约定**：禁止 `!`、用 `Logger` 不用 `print`、`try-catch`、ScrollView 隐藏滚动条、日期用 `DateFormatter` + `zh_CN`、SF Symbol 新增前先 `NSImage(systemSymbolName:)` 验证。
