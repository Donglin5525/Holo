# HoloAI 双层记忆架构 — 实施方案

> 日期：2026-06-03
> 依据：`docs/_common/plans/2026-06-03-HoloAI双层记忆架构方案.md`（方案 v1 + 三轮审查）
> 范围：Phase 0 地基修复 + Phase 1 Habit/Goal 端到端薄切片
> 状态：待实施

---

## Phase 0：地基修复

> 目标：修复已知 bug、拆分 feature flag、补齐 Store 测试，为 Phase 1 清地基。
> 不接 LLM，不改变用户体验。

### Task 0.1：修复 HoloLongTermMemoryStore 日期解码 bug

**问题**：`save()` 第 67 行显式 `encoder.dateEncodingStrategy = .iso8601`，但 `load()` 第 39 行 `JSONDecoder()` 未设置对应解码策略。

**文件**：`Services/AI/HoloLongTermMemoryStore.swift`

**改动**：

```swift
// 第 39 行，将：
let memories = try JSONDecoder().decode([HoloLongTermMemory].self, from: data)

// 改为：
let decoder = JSONDecoder()
decoder.dateDecodingStrategy = .iso8601
let memories = try decoder.decode([HoloLongTermMemory].self, from: data)
```

**测试**：新增 `HoloLongTermMemoryStoreTests.swift`

```
- testSaveAndLoad_日期字段保持一致
  1. 创建含特定 Date 的 HoloLongTermMemory
  2. save()
  3. load()
  4. 断言所有 Date 字段与原始值相等（精确到秒）

- testLoad_损坏文件时返回空数组不崩溃
  1. 写入非法 JSON 到 Store 文件
  2. load()
  3. 断言返回空数组，无 throw

- testLoad_文件不存在时返回空数组
  1. 删除 Store 文件
  2. load()
  3. 断言返回空数组
```

**验收**：测试通过 + 现有长期记忆功能不受影响。

---

### Task 0.2：拆分 Feature Flags

**问题**：`memoryInsightCandidateExtractionEnabled` 硬绑定 `longTermMemoryEnabled`；`memoryInsightExtractionEnabled` 字段存在但未被使用。

**文件**：`Models/AI/HoloAICapability.swift`

**改动**：

1. `HoloMemorySettings` 新增字段：

```swift
// 新增
@Published var episodicMemoryObservationEnabled: Bool {
    didSet { defaults.set(episodicMemoryObservationEnabled, forKey: Keys.episodicMemoryObservationEnabled) }
}

// Keys 新增
static let episodicMemoryObservationEnabled = "holo_memory_episodicObservationEnabled"

// init 新增
self.episodicMemoryObservationEnabled = defaults.object(forKey: Keys.episodicMemoryObservationEnabled) as? Bool ?? false
```

2. `HoloAIFeatureFlags` 改为：

```swift
enum HoloAIFeatureFlags {
    static var capabilityLaunchpadEnabled: Bool { true }
    static var memorySummaryInjectionEnabled: Bool { HoloMemorySettings.shared.memorySummaryInjectionEnabled }
    static var longTermMemoryWriteEnabled: Bool { HoloMemorySettings.shared.longTermMemoryEnabled }
    static var memoryInsightCandidateExtractionEnabled: Bool { HoloMemorySettings.shared.longTermMemoryEnabled }
    // 新增
    static var episodicMemoryObservationEnabled: Bool { HoloMemorySettings.shared.episodicMemoryObservationEnabled }
}
```

3. `memoryInsightExtractionEnabled` 标记废弃：

```swift
@available(*, deprecated, message: "未使用，请使用 episodicMemoryObservationEnabled")
@Published var memoryInsightExtractionEnabled: Bool { ... }
```

**验收**：编译通过 + 现有开关行为不变。

---

### Task 0.3：旧长期记忆 JSON Schema 兼容测试

**文件**：新增 `HoloLongTermMemoryStoreTests.swift`（同 Task 0.1）

**测试**：

```
- testLoad_OldSchema_缺失字段不崩溃
  1. 写入只含 id/title/summary/createdAt/updatedAt 的最小 JSON
  2. load()
  3. 断言可选字段为 nil/默认值，不崩溃

- testSaveAndLoad_SchemaVersion兼容
  1. 模拟 v1 JSON（无 schemaVersion 字段）
  2. load() 成功
  3. save() 写入后包含所有字段
  4. 再次 load() 成功
```

**验收**：旧数据可正常加载。

---

## Phase 1A：Store + Signal Builder + Package Builder

> 目标：建立数据层基础，为 Observer 提供输入和输出容器。

### Task 1A.1：新建 HoloEpisodicMemoryModels

**文件**：新增 `Models/AI/HoloEpisodicMemoryModels.swift`

```swift
import Foundation

// MARK: - Episodic Memory

struct HoloEpisodicMemory: Codable, Equatable, Identifiable {
    var id: String
    var title: String
    var summary: String
    var state: HoloEpisodicMemoryState
    var visibility: HoloMemoryVisibility
    var confidence: HoloMemoryConfidence
    var sensitivity: HoloMemorySensitivity
    var hitCount: Int
    var semanticHitRunIDs: [String]
    var evidence: [HoloLongTermMemoryEvidence]  // 复用现有证据模型
    var createdAt: Date
    var updatedAt: Date
    var lastHitAt: Date?
    var expiresAt: Date
    var sourceModules: [HoloMemorySource]  // 来源模块（Habit/Goal）
    var reasoningSummary: String?
    var userEditedSummary: String?
    var promotedLongTermMemoryID: String?

    // 审计
    var createdFromRunID: String?
    var schemaVersion: Int = 1
}

// MARK: - State Machine

enum HoloEpisodicMemoryState: String, Codable {
    case observing
    case active
    case suggested
    case promotionCandidate
    case promoted
    case rejected
    case expired
    case archived
}

// 复用现有枚举
// HoloMemoryVisibility: hidden / suggested / reviewRequired（需新增到 HoloLongTermMemoryModels.swift 或独立文件）
// HoloMemoryConfidence: low / medium / high（已有）
// HoloMemorySensitivity: normal / highImpact / sensitive（已有）
// HoloMemorySource（已有）
```

**同时新增**：`HoloMemoryVisibility` 枚举（如果尚不存在）

```swift
enum HoloMemoryVisibility: String, Codable {
    case hidden
    case suggested
    case reviewRequired
}
```

**验收**：编译通过。

---

### Task 1A.2：新建 HoloEpisodicMemoryStore

**文件**：新增 `Services/AI/HoloEpisodicMemoryStore.swift`

**设计参考**：复用 `HoloLongTermMemoryStore` 的文件存储模式（JSON 文件 + 原子写入 + 损坏备份），但**必须同时设置 encoder 和 decoder 的 dateEncodingStrategy**。

```swift
import Foundation
import OSLog

final class HoloEpisodicMemoryStore {
    static let shared = HoloEpisodicMemoryStore()

    private let logger = Logger(subsystem: "com.holo.app", category: "EpisodicMemoryStore")
    private let fileManager = FileManager.default
    private let storeURL: URL
    private let backupURL: URL
    private let queue = DispatchQueue(label: "com.holo.episodicMemoryStore", attributes: .concurrent)

    // 90 天硬上限
    static let maxLifetimeDays: Int = 90

    private init() {
        let dir = fileManager.urls(for: .applicationSupportDirectory, in: .userDomainMask)[0]
            .appendingPathComponent("Holo/Memory", isDirectory: true)
        try? fileManager.createDirectory(at: dir, withIntermediateDirectories: true)
        storeURL = dir.appendingPathComponent("episodicMemories.json")
        backupURL = dir.appendingPathComponent("episodicMemories.backup.json")
    }

    // MARK: - CRUD

    func load() -> [HoloEpisodicMemory] { ... }
    func save(_ memories: [HoloEpisodicMemory]) { ... }
    func upsert(_ memory: HoloEpisodicMemory) { ... }
    func updateState(id: String, to newState: HoloEpisodicMemoryState) { ... }
    func delete(id: String) { ... }
    func reject(id: String) -> HoloMemorySuppressionRule? { ... }
    func markExpired() -> [String] { ... }  // 批量过期超期记忆
    func loadSuppressionRules() -> [HoloMemorySuppressionRule] { ... }
    func saveSuppressionRules(_ rules: [HoloMemorySuppressionRule]) { ... }

    // MARK: - Query

    func queryActive() -> [HoloEpisodicMemory] { ... }         // state == active/suggested
    func querySuggested() -> [HoloEpisodicMemory] { ... }      // state == suggested
    func queryByRunID(_ runID: String) -> [HoloEpisodicMemory] { ... }

    // MARK: - Private

    private func encode(_ memories: [HoloEpisodicMemory]) throws -> Data {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .iso8601
        encoder.outputFormatting = [.sortedKeys]
        return try encoder.encode(memories)
    }

    private func decode(_ data: Data) -> [HoloEpisodicMemory] {
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601  // 必须与 encoder 一致
        return (try? decoder.decode([HoloEpisodicMemory].self, from: data)) ?? []
    }
}
```

**Suppression Rule 模型**：

```swift
struct HoloMemorySuppressionRule: Codable, Equatable, Identifiable {
    var id: String
    var originalMemorySummary: String
    var keywordGroups: [[String]]   // 关联词组：[["抽烟","烟草","戒烟"], ["宵夜","夜间","深夜"]]
    var suppressedUntil: Date
    var originalRejectedAt: Date
}
```

**测试**：新增 `HoloEpisodicMemoryStoreTests.swift`

```
- testSaveAndLoad_日期策略一致
- testSaveAndLoad_空Store往返
- testUpsert_新增与更新
- testUpdateState_状态转换
- testMarkExpired_超期记忆自动过期
- testReject_生成SuppressionRule
- testLoadSuppressionRules_过期规则自动清理
- test损坏文件_备份恢复
```

**验收**：全部测试通过。

---

### Task 1A.3：Habit Signal Builder

**文件**：新增 `Services/AI/MemorySignals/HabitMemorySignalBuilder.swift`

**复用现有数据**：
- `HabitFocusSignal`（`Models/HabitFocusModels.swift` 第 24-64 行）
- `HabitPerformanceSnapshot`（`Models/HabitPerformanceModels.swift` 第 21-33 行）

```swift
import Foundation

struct HabitMemorySignalBuilder {
    /// 从习惯数据生成记忆信号
    /// - Parameters:
    ///   - days: 回溯天数，默认 14
    ///   - completion: HabitRepository 的完成数据
    /// - Returns: 高密度信号列表
    static func buildSignals(days: Int = 14) async -> [HoloMemorySignal] {
        // 1. 获取负向习惯（坏习惯）的最近 N 天表现
        // 2. 获取正向习惯的中断和连续情况
        // 3. 对每个习惯生成统计摘要（频次、趋势、是否超标）
        // 4. 只保留 confidence > 0.4 的信号
        // 5. 每条信号附带 evidenceRefs（具体哪几天、多少次）
    }
}
```

**信号生成规则**（代码规则，不涉及 LLM）：

| 触发条件 | 信号 | polarity | confidence |
|---------|------|----------|-----------|
| 坏习惯 7 天内超标 ≥ 3 天 | "XX习惯控制不稳定" | negative | 0.7+ |
| 好习惯连续中断 ≥ 5 天 | "XX习惯近期中断" | negative | 0.6+ |
| 好习惯连续完成 ≥ 7 天 | "XX习惯连续保持" | positive | 0.5+ |
| 坏习惯控制率提升 ≥ 20% | "XX习惯控制改善" | positive | 0.6+ |

**验收**：能从现有习惯数据生成 3-5 条信号，每条有 evidenceRefs。

---

### Task 1A.4：Goal Signal Builder

**文件**：新增 `Services/AI/MemorySignals/GoalMemorySignalBuilder.swift`

**复用现有数据**：
- `GoalRepository.activeGoalsForAI(limit:)`（第 33-42 行）
- `GoalRepository.completedGoalsCount(from:to:)`（第 45-54 行）

```swift
import Foundation

struct GoalMemorySignalBuilder {
    static func buildSignals(days: Int = 14) async -> [HoloMemorySignal] {
        // 1. 获取活跃目标及其进度
        // 2. 计算目标完成率、剩余天数、进度趋势
        // 3. 标记落后目标（进度 < 预期进度 × 0.7）
        // 4. 标记即将到期目标
        // 5. 每条信号附带 evidenceRefs
    }
}
```

**信号生成规则**：

| 触发条件 | 信号 | polarity | confidence |
|---------|------|----------|-----------|
| 进度 < 预期 × 0.5 | "XX目标严重落后" | negative | 0.8+ |
| 进度 < 预期 × 0.7 | "XX目标进度偏慢" | negative | 0.6+ |
| 剩余 < 7 天且未完成 | "XX目标即将到期" | mixed | 0.7+ |
| 已完成 | "XX目标已完成" | positive | 0.9+ |

**验收**：能从现有目标数据生成信号，进度落后目标有明确 evidenceRefs。

---

### Task 1A.5：Observation Package Builder

**文件**：新增 `Services/AI/MemoryObserver/HoloObservationPackageBuilder.swift`

```swift
import Foundation

struct HoloObservationPackageBuilder {
    /// MVP 只支持 Habit + Goal 信号
    static func buildPackage(
        habitSignals: [HoloMemorySignal],
        goalSignals: [HoloMemorySignal],
        existingEpisodicMemories: [HoloEpisodicMemory],
        existingLongTermMemories: [HoloLongTermMemory],
        suppressionRules: [HoloMemorySuppressionRule],
        memoryFeedbackHistory: [HoloMemoryFeedbackEntry]
    ) -> HoloObservationPackage {
        // 1. 生成 runID（obs-yyyy-MM-dd-HHmmss）
        // 2. 组装各 section
        // 3. 按 token budget 裁剪（MVP: 12k 上限）
        // 4. 返回序列化后的 package
    }
}

// Token 预算
struct HoloObservationPackageLimits {
    static let maxTotalEstimatedTokens = 12_000
    static let habitSignalsMaxCount = 20
    static let goalSignalsMaxCount = 10
    static let existingEpisodicMemoriesMaxCount = 20
    static let existingLongTermMemoriesMaxCount = 10
    static let suppressionRulesMaxCount = 20
}

// 观察包结构
struct HoloObservationPackage: Codable {
    var runID: String
    var period: ObservationPeriod
    var habitSignals: [HoloMemorySignal]
    var goalSignals: [HoloMemorySignal]
    var existingEpisodicMemories: [HoloEpisodicMemorySummary]  // 只传摘要
    var existingLongTermMemories: [HoloLongTermMemorySummary]   // 只传摘要
    var memoryFeedbackHistory: [HoloMemoryFeedbackEntry]
    var suppressionRules: [HoloMemorySuppressionRule]
    var estimatedTokens: Int
}

struct ObservationPeriod: Codable {
    var start: String  // ISO8601
    var end: String
    var window: String  // "fourteenDays"
}

struct HoloEpisodicMemorySummary: Codable {
    var id: String
    var title: String
    var summary: String
    var state: HoloEpisodicMemoryState
    var hitCount: Int
    var lastHitAt: String?
}

struct HoloLongTermMemorySummary: Codable {
    var id: String
    var title: String
    var summary: String
}

struct HoloMemoryFeedbackEntry: Codable {
    var memoryID: String
    var action: String  // "rejected" / "deleted" / "edited"
    var originalSummary: String?
    var editedSummary: String?
    var timestamp: String
}
```

**Token 裁剪逻辑**：

```
1. 估算各 section token 数（CJK 字符 × 1.5）
2. 如果总 token > 12k：
   a. 按优先级排序：信号 > 既有记忆 > 反馈历史
   b. 信号按 confidence 降序截断
   c. 既有记忆按 lastHitAt 降序截断
3. 在 runID 中标记是否 truncated
```

**验收**：能组装观察包，超限时正确截断，token 估算 ≤ 12k。

---

## Phase 1B：Observer Prompt + Parser + Validator + Applier

> 目标：建立 LLM Memory Observer 的安全调用链路，能从观察包生成短期记忆。

### Task 1B.1：iOS 端 PromptManager 新增 memory_observer

**文件**：修改 `Services/AI/PromptManager.swift`

**改动**：

1. `PromptType` 枚举新增 case：

```swift
case memoryObserver = "memory_observer"
```

2. `templates` 字典新增模板（后备版本）：

```
你是 HoloAI 的记忆观察引擎。你会收到一个观察包，包含用户近期的模块信号和既有记忆。

你的任务是：
1. 判断哪些模式值得形成新的短期记忆（Episodic Memory）。
2. 判断哪些既有短期记忆被当前信号语义命中（仍相关）。
3. 判断哪些既有短期记忆应该被标记为弱化/过期。

安全约束（必须遵守）：
- 不把短期倾向写成永久事实。
- 不根据单次行为推断人格、身份、医疗或心理状态。
- 对坏习惯、健康、金钱压力等高影响内容使用克制措辞。
- 用户否定过的内容是反例，不得换个说法重复提出。
- 每条输出必须有 evidenceRefs，且 evidenceRefs 必须在输入信号中存在。
- 只输出 suggested 或 active 状态的记忆，不输出 promotionCandidate。
- 既有记忆与原始信号冲突时，以原始信号为准。

输出 JSON 格式：
{
  "newEpisodicMemories": [...],
  "memoryHits": [...],
  "weakenedOrExpiredMemories": [...]
}

每条新记忆必须包含：title, memoryText, confidence (0.0-1.0), sensitivity, visibility, evidenceRefs, reasoningSummary, expiresInDays (7-90)
```

3. `promptVersions` 新增版本号：

```swift
.memoryObserver: 1
```

**验收**：`PromptManager.shared.loadTemplate(for: .memoryObserver)` 返回有效模板。

---

### Task 1B.2：后端新增 memory_observer prompt

**文件**：
- 修改 `HoloBackend/src/prompts/defaultPrompts.json`：新增 `"memory_observer"` 键，内容与 iOS 后备模板一致
- 修改 `HoloBackend/src/prompts/promptRegistry.js`：无需额外改动（`PROMPT_TYPES` 从 defaultPrompts.json 动态推导）

**部署**：Phase 1 交付前必须执行后端部署（`/holo-backend-deploy`）。

**验证**：

```bash
curl http://localhost:8787/v1/prompts/memory_observer
```

确认返回有效 JSON，version 为 1。

---

### Task 1B.3：Observer Response Parser

**文件**：新增 `Services/AI/MemoryObserver/HoloMemoryObserverResponseParser.swift`

```swift
import Foundation

struct HoloMemoryObserverOutput: Codable {
    var newEpisodicMemories: [NewEpisodicMemoryEntry]
    var memoryHits: [MemoryHitEntry]
    var weakenedOrExpiredMemories: [WeakenedEntry]
}

struct NewEpisodicMemoryEntry: Codable {
    var title: String
    var memoryText: String
    var confidence: Double
    var sensitivity: String
    var visibility: String
    var evidenceRefs: [String]  // 对应信号 ID
    var reasoningSummary: String
    var expiresInDays: Int
}

struct MemoryHitEntry: Codable {
    var episodicMemoryID: String
    var hitReasoning: String
}

struct WeakenedEntry: Codable {
    var episodicMemoryID: String
    var reason: String
}

enum HoloMemoryObserverResponseParser {
    /// 解析 Observer LLM 输出
    /// - Returns: 解析成功返回 output，失败返回 nil（不 throw，失败兜底）
    static func parse(_ rawResponse: String) -> HoloMemoryObserverOutput? {
        // 1. 提取 JSON（可能被 markdown code block 包裹）
        // 2. JSONDecoder + .iso8601 解码
        // 3. 解码失败返回 nil（记录日志）
    }
}
```

**验收**：能解析正常 JSON、被 ``` 包裹的 JSON、空 JSON。

---

### Task 1B.4：Observer Output Validator

**文件**：新增 `Services/AI/MemoryObserver/MemoryObserverOutputValidator.swift`

```swift
import Foundation

struct MemoryObserverValidationResult {
    var validNewMemories: [ValidatedNewMemory]
    var validHits: [MemoryHitEntry]
    var validWeakened: [WeakenedEntry]
    var rejectedEntries: [RejectedEntry]
}

struct RejectedEntry {
    var reason: String
    var rawData: String?
}

struct ValidatedNewMemory {
    var title: String
    var summary: String
    var confidence: Double
    var sensitivity: HoloMemorySensitivity
    var visibility: HoloMemoryVisibility
    var evidenceRefs: [String]  // 已验证存在于输入信号中
    var reasoningSummary: String
    var expiresInDays: Int
}

enum MemoryObserverOutputValidator {
    static func validate(
        _ output: HoloMemoryObserverOutput,
        against package: HoloObservationPackage,
        suppressionRules: [HoloMemorySuppressionRule]
    ) -> MemoryObserverValidationResult {
        var result = MemoryObserverValidationResult(...)

        for entry in output.newEpisodicMemories {
            // 校验 1：confidence 范围 [0.0, 1.0]
            guard entry.confidence >= 0.0, entry.confidence <= 1.0 else { reject; continue }

            // 校验 2：confidence 最低阈值 0.4
            guard entry.confidence >= 0.4 else { reject; continue }

            // 校验 3：evidenceRefs 必须在输入信号的 ID 中存在
            let allSignalIDs = package.habitSignals.map(\.id) + package.goalSignals.map(\.id)
            let validRefs = entry.evidenceRefs.filter { allSignalIDs.contains($0) }
            guard !validRefs.isEmpty else { reject; continue }

            // 校验 4：expiresInDays 范围 [7, 90]
            guard entry.expiresInDays >= 7, entry.expiresInDays <= 90 else { reject; continue }

            // 校验 5：敏感记忆 visibility 不能是 hidden
            if entry.sensitivity == "sensitive" || entry.sensitivity == "highImpact" {
                guard entry.visibility != "hidden" else { reject; continue }
            }

            // 校验 6：suppression 关键词匹配
            let isSuppressed = suppressionRules.contains { rule in
                let now = Date()
                guard rule.suppressedUntil > now else { return false }
                return rule.keywordGroups.contains { group in
                    group.contains { keyword in
                        entry.memoryText.contains(keyword) || entry.title.contains(keyword)
                    }
                }
            }
            guard !isSuppressed else { reject; continue }

            // 通过所有校验
            result.validNewMemories.append(...)
        }

        // memoryHits 和 weakened 校验：episodicMemoryID 必须在 existingEpisodicMemories 中存在
        ...

        return result
    }
}
```

**测试**：新增 `MemoryObserverOutputValidatorTests.swift`

```
- testValidate_正常输出_全部通过
- testValidate_非法confidence_被过滤
- testValidate_低confidence_被过滤
- testValidate_幻觉evidenceRefs_被过滤
- testValidate_敏感记忆hidden_被过滤
- testValidate_suppression命中_被拦截
- testValidate_expiresInDays超范围_被过滤
- testValidate_部分通过_混合结果
- testValidate_emptyOutput_返回空结果
```

**验收**：全部测试通过。

---

### Task 1B.5：Observation Applier

**文件**：新增 `Services/AI/MemoryObserver/HoloMemoryObservationApplier.swift`

```swift
import Foundation
import OSLog

/// 将 Validator 校验通过的 Observer 输出应用到 Store
/// 职责：状态机转换、并发锁、去重、审计日志
final class HoloMemoryObservationApplier {
    private let logger = Logger(subsystem: "com.holo.app", category: "MemoryApplier")
    private let store = HoloEpisodicMemoryStore.shared

    /// 应用一次观察结果
    /// - Returns: 实际写入/更新的记忆数量
    @discardableResult
    func apply(
        validationResult: MemoryObserverValidationResult,
        runID: String
    ) -> ApplyResult {
        // 检查 feature flag
        guard HoloAIFeatureFlags.episodicMemoryObservationEnabled else {
            logger.info("Observer 已关闭，跳过 apply")
            return ApplyResult(newCount: 0, hitCount: 0, expiredCount: 0)
        }

        var newCount = 0
        var hitCount = 0

        // 1. 写入新记忆
        for memory in validationResult.validNewMemories {
            let episodic = HoloEpisodicMemory(
                id: UUID().uuidString,
                title: memory.title,
                summary: memory.summary,
                state: memory.visibility == .suggested ? .suggested : .active,
                visibility: memory.visibility,
                confidence: ...,
                sensitivity: memory.sensitivity,
                hitCount: 0,
                semanticHitRunIDs: [runID],
                evidence: ...,  // 从信号 evidenceRefs 构造
                createdAt: Date(),
                updatedAt: Date(),
                lastHitAt: nil,
                expiresAt: Calendar.current.date(byAdding: .day, value: memory.expiresInDays, to: Date())!,
                sourceModules: [...],
                reasoningSummary: memory.reasoningSummary,
                createdFromRunID: runID
            )

            // 去重检查：相似 title + 相似 summary 的记忆不重复创建
            let existing = store.load()
            let isDuplicate = existing.contains { mem in
                mem.title == episodic.title && mem.state != .expired && mem.state != .rejected
            }
            guard !isDuplicate else { continue }

            store.upsert(episodic)
            newCount += 1
        }

        // 2. 处理命中（续期）
        for hit in validationResult.validHits {
            var memories = store.load()
            guard let index = memories.firstIndex(where: { $0.id == hit.episodicMemoryID }) else { continue }
            let original = memories[index]

            // 状态机检查：只有 active/suggested 可以被命中续期
            guard original.state == .active || original.state == .suggested else { continue }

            // 续期：14 天，但不超过 90 天硬上限
            let renewedExpiry = Calendar.current.date(byAdding: .day, value: 14, to: Date())!
            let maxExpiry = Calendar.current.date(byAdding: .day, value: HoloEpisodicMemoryStore.maxLifetimeDays, to: original.createdAt)!
            let finalExpiry = min(renewedExpiry, maxExpiry)

            memories[index].hitCount += 1
            memories[index].semanticHitRunIDs.append(runID)
            memories[index].lastHitAt = Date()
            memories[index].updatedAt = Date()
            memories[index].expiresAt = finalExpiry

            store.save(memories)
            hitCount += 1
        }

        // 3. 处理弱化/过期
        for weakened in validationResult.validWeakened {
            // 标记为 expired 或直接删除（根据 reason）
            ...
        }

        return ApplyResult(newCount: newCount, hitCount: hitCount, expiredCount: ...)
    }
}

struct ApplyResult {
    var newCount: Int
    var hitCount: Int
    var expiredCount: Int
}
```

**测试**：新增 `HoloMemoryObservationApplierTests.swift`

```
- testApply_新记忆写入成功
- testApply_重复记忆不写入
- testApply_命中续期不超过90天硬上限
- testApply_featureFlag关闭时不写入
- testApply_expired记忆不能被命中续期
- testApply_rejected记忆不能被命中续期
```

**验收**：全部测试通过。

---

### Task 1B.6：Memory Observer Service（调度器）

**文件**：新增 `Services/AI/HoloMemoryObserverService.swift`

```swift
import Foundation
import OSLog

/// Memory Observer 调度器：组装观察包 → 调用 LLM → 解析 → 校验 → 应用
final class HoloMemoryObserverService {
    static let shared = HoloMemoryObserverService()

    private let logger = Logger(subsystem: "com.holo.app", category: "MemoryObserver")

    private init() {}

    /// 执行一次完整观察
    func runObservation() async {
        // 1. 检查 feature flag
        guard HoloAIFeatureFlags.episodicMemoryObservationEnabled else { return }

        // 2. 构建信号
        let habitSignals = await HabitMemorySignalBuilder.buildSignals()
        let goalSignals = await GoalMemorySignalBuilder.buildSignals()

        guard !habitSignals.isEmpty || !goalSignals.isEmpty else {
            logger.info("无有效信号，跳过观察")
            return
        }

        // 3. 组装观察包
        let package = HoloObservationPackageBuilder.buildPackage(
            habitSignals: habitSignals,
            goalSignals: goalSignals,
            existingEpisodicMemories: HoloEpisodicMemoryStore.shared.load(),
            existingLongTermMemories: HoloLongTermMemoryStore.shared.load(),
            suppressionRules: HoloEpisodicMemoryStore.shared.loadSuppressionRules(),
            memoryFeedbackHistory: []  // Phase 1 暂不实现
        )

        // 4. 调用 LLM（通过 HoloBackendAIProvider）
        let prompt = PromptManager.shared.loadTemplate(for: .memoryObserver)
        let userMessage = encodePackageAsUserMessage(package)

        let rawResponse: String
        do {
            rawResponse = try await HoloBackendAIProvider.shared.callObserver(userMessage: userMessage, systemPrompt: prompt)
        } catch {
            logger.error("Observer LLM 调用失败: \(error)")
            return  // 静默失败，不阻塞主流程
        }

        // 5. 解析
        guard let output = HoloMemoryObserverResponseParser.parse(rawResponse) else {
            logger.error("Observer 输出解析失败")
            return
        }

        // 6. 校验
        let validationResult = MemoryObserverOutputValidator.validate(
            output,
            against: package,
            suppressionRules: HoloEpisodicMemoryStore.shared.loadSuppressionRules()
        )

        logger.info("Observer 校验结果: \(validationResult.validNewMemories.count) 新记忆, \(validationResult.rejectedEntries.count) 被拒绝")

        // 7. 应用
        let applier = HoloMemoryObservationApplier()
        let result = applier.apply(
            validationResult: validationResult,
            runID: package.runID
        )

        logger.info("Observer 应用结果: \(result.newCount) 新增, \(result.hitCount) 命中, \(result.expiredCount) 过期")

        // 8. 记录 run 结果（用于调试和成本追踪）
        saveRunRecord(...)
    }

    private func encodePackageAsUserMessage(_ package: HoloObservationPackage) -> String {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .iso8601
        guard let data = try? encoder.encode(package),
              let json = String(data: data, encoding: .utf8) else {
            return "{}"
        }
        return json
    }

    private func saveRunRecord(runID: String, package: HoloObservationPackage, result: ApplyResult, error: String?) {
        // 保存到 HoloMemoryObservationRunStore
        // 只保存统计摘要，不保存敏感原文
    }
}
```

**HoloBackendAIProvider 扩展**：新增 `callObserver` 方法，走后端 `/v1/ai/chat/completions` 端点，使用 `memory_observer` prompt type。

**触发时机（Phase 1）**：
- 每天凌晨后台执行一次（通过 BGTaskScheduler 或 App 进入后台时延迟触发）
- 用户打开记忆中心时手动刷新一次

**验收**：完整链路跑通（信号 → 观察包 → LLM → 解析 → 校验 → 写入 Store）。

---

## Phase 1C：Memory Center "最近记住"区

> 目标：用户可以在记忆中心看到短期记忆、查看证据、隐藏/删除/拒绝。

### Task 1C.1：改版 HoloMemoryCenterView

**文件**：修改 `Views/AI/HoloMemoryCenterView.swift`

**改动**：从两区改为三区：

```swift
// Section 1: 最近记住（Episodic Memory）
// 展示 state == .suggested 的短期记忆
// 每条显示：title + summary + 来源模块图标 + 过期倒计时
// 操作：查看详情、隐藏、删除

// Section 2: 待确认（Long-term Candidate）
// 现有逻辑不变

// Section 3: 已记住（Confirmed Long-term）
// 现有逻辑不变
```

**短期记忆卡片 UI**：

```
┌─────────────────────────────┐
│ 🔵 最近在控制烟草消费        │
│ 习惯数据显示控制率有所提升    │
│ 📊 2 条证据  ·  ⏳ 12 天后过期 │
│                   [隐藏] [×]  │
└─────────────────────────────┘
```

**验收**：记忆中心显示三区，短期记忆可查看详情（含证据列表）、可隐藏/删除。

---

### Task 1C.2：短期记忆详情 Sheet

**文件**：新增 `Views/AI/HoloEpisodicMemoryDetailView.swift`

**展示内容**：
1. 记忆标题和摘要
2. 为什么这么认为（reasoningSummary）
3. 证据列表（来自哪个模块、什么时间、什么数据）
4. 命中历史（第几次被续期、最近一次来自哪个 run）
5. 过期时间
6. 操作按钮：隐藏、删除、拒绝（拒绝时生成 suppression rule）

**验收**：详情页展示所有信息，拒绝后同类记忆 30 天内不再出现。

---

## 后端改动汇总

| 文件 | 改动 |
|------|------|
| `HoloBackend/src/prompts/defaultPrompts.json` | 新增 `memory_observer` prompt |
| `HoloBackend/src/prompts/promptRegistry.js` | 无需改动（自动推导） |
| `HoloBackend/src/config.js` | 可选：新增 Observer 模型路由配置 |
| `HoloBackend/src/app.js` | 无需改动（使用现有 `/v1/ai/chat/completions` 端点） |

**部署**：Phase 1B 完成后执行 `/holo-backend-deploy`。

---

## iOS 新增文件汇总

| 文件路径 | Phase | 说明 |
|---------|-------|------|
| `Models/AI/HoloEpisodicMemoryModels.swift` | 1A | 短期记忆数据模型 |
| `Services/AI/HoloEpisodicMemoryStore.swift` | 1A | 短期记忆 JSON Store |
| `Services/AI/MemorySignals/HabitMemorySignalBuilder.swift` | 1A | 习惯信号构建器 |
| `Services/AI/MemorySignals/GoalMemorySignalBuilder.swift` | 1A | 目标信号构建器 |
| `Services/AI/MemoryObserver/HoloObservationPackageBuilder.swift` | 1A | 观察包构建器 |
| `Services/AI/MemoryObserver/HoloMemoryObserverResponseParser.swift` | 1B | Observer 输出解析器 |
| `Services/AI/MemoryObserver/MemoryObserverOutputValidator.swift` | 1B | Observer 输出校验器 |
| `Services/AI/MemoryObserver/HoloMemoryObservationApplier.swift` | 1B | 观察结果应用器 |
| `Services/AI/HoloMemoryObserverService.swift` | 1B | Observer 调度服务 |
| `Views/AI/HoloEpisodicMemoryDetailView.swift` | 1C | 短期记忆详情页 |
| `Services/AI/HoloMemoryObservationRunStore.swift` | 1B | 观察运行记录 Store |

## iOS 修改文件汇总

| 文件路径 | Phase | 改动 |
|---------|-------|------|
| `Services/AI/HoloLongTermMemoryStore.swift` | 0 | 修复日期解码策略 |
| `Models/AI/HoloAICapability.swift` | 0 | 拆分 feature flag、新增 episodicMemoryObservationEnabled |
| `Services/AI/PromptManager.swift` | 1B | 新增 memoryObserver prompt type |
| `Services/AI/HoloBackendAIProvider.swift` | 1B | 新增 callObserver 方法 |
| `Views/AI/HoloMemoryCenterView.swift` | 1C | 三区改版 |

## 测试文件汇总

| 文件路径 | Phase |
|---------|-------|
| `HoloLongTermMemoryStoreTests.swift` | 0 |
| `HoloEpisodicMemoryStoreTests.swift` | 1A |
| `MemoryObserverOutputValidatorTests.swift` | 1B |
| `HoloMemoryObservationApplierTests.swift` | 1B |

---

## 验收 Checklist

### Phase 0

- [ ] `HoloLongTermMemoryStore.load()` 解码日期不再失败
- [ ] 旧 JSON 文件可正常加载
- [ ] `episodicMemoryObservationEnabled` 存在且被 `HoloAIFeatureFlags` 真实检查
- [ ] `memoryInsightExtractionEnabled` 标记为 deprecated
- [ ] 编译通过，现有功能不受影响

### Phase 1A

- [ ] `HoloEpisodicMemoryStore` 保存/读取/过期/拒绝全链路测试通过
- [ ] Habit Signal Builder 能从现有习惯数据生成信号
- [ ] Goal Signal Builder 能从现有目标数据生成信号
- [ ] Observation Package 总 token ≤ 12k
- [ ] 编译通过

### Phase 1B

- [ ] Observer LLM 调用成功并返回结构化 JSON
- [ ] Parser 能解析正常/异常输出
- [ ] Validator 拦截非法 confidence、幻觉 evidenceRefs、suppression 命中
- [ ] Applier 正确写入 Store、去重、续期不超 90 天硬上限
- [ ] feature flag 关闭时全链路静默跳过
- [ ] 后端部署完成，`curl /v1/prompts/memory_observer` 返回有效 prompt
- [ ] 单元测试全部通过

### Phase 1C

- [ ] 记忆中心显示三区
- [ ] 短期记忆卡片展示 title/summary/来源/过期倒计时
- [ ] 详情页展示 reasoningSummary + 证据列表 + 命中历史
- [ ] 隐藏/删除/拒绝操作正常
- [ ] 拒绝后同类记忆 30 天内不再生成
