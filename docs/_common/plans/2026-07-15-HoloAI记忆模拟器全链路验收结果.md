# HoloAI 记忆模拟器全链路验收结果

## 结论

- 场景：`full-chain-v1`
- 结果：`passed`
- 断言：33 通过，0 失败
- 执行时间：2026-07-15 17:41:36–17:41:37（Asia/Shanghai）
- 模拟器：iPhone 17 Pro / iOS 26.3
- UDID：`2F63C1D8-E4DF-48AC-A6A0-503D9C72E1D8`

本次不是静态假页面：固定模型 JSON 实际经过 Validator、Observation Applier、隔离 Core Data Repository、Feedback、Forgetting、Cross-domain Fusion、Query Service 和 Context Envelope。

## 核心结果

| 验收项 | 结果 |
|---|---|
| `requestedActions:null` | Validator 接受，无拒绝 |
| 领域记忆 | 财务、健康直接激活；习惯候选经“准确”后激活 |
| 用户反馈 | 准确、纠正、不准确、不再使用均按预期落库 |
| 纠正 | 稳定 ID 不变，版本 +1，状态为 `active/corrected` |
| 忘记 | 正文和 evidence 被清空，semantic tombstone 阻止同义重建 |
| 跨域融合 | 财务 + 健康通过四道门并形成综合观察 |
| 幂等 | 同 observation key 被识别为重复；无 reset 重启不重放反馈 |
| 关闭辅助 | 不读取记忆、不刷新、不生成上下文 |
| 排除规则 | rejected 和 forgotten 记录不会进入回答上下文 |

最终隔离仓库包含 7 个身份状态：5 条 active、1 条 suppressed、1 条 tombstoned。记忆管理 UI 只展示 5 条 active 记录。

## 查询路由结果

| 问题 | 路由 | 选中 | 结论 |
|---|---|---:|---|
| 我最近状态如何 | `holisticMemory` | 5 | 可使用跨域、习惯、观点、健康、财务记忆 |
| 我的财务状态怎么样 | `domainMemory` | 1 | 只使用财务记忆 |
| 最近14天吃了多少麦当劳 | `detail` | 1 条背景 | 必须查询业务明细，记忆不能提供精确次数或金额 |

本地回答上下文已经通过生产 `HoloMemoryQueryService` 和 `HoloMemoryContextEnvelope` 生成；普通聊天、深度 Agent 和各能力入口均从同一 Query Service 读取。当前模拟器的 HoloAI 数据处理授权为关闭状态，因此本次没有把 Mock 上下文发送给外部模型，也不把“没有真实模型回复”误报为本地记忆链失败。

## UI 验证

路径：`首页头像 → 设置 → AI 记忆 → 记忆管理 → 点击记录`。

- 记忆管理可见财务、观点、健康、习惯、综合观察各 1 条。
- 财务详情可见来源和模拟证据。
- 详情底部真实存在“准确 / 不准确 / 纠正 / 不再使用”四个入口。
- 验收结束时未点击上述入口，模拟器停留在财务记忆详情页，供产品负责人复查。

## 构建与隔离

- Debug 指定模拟器构建：通过。
- Release iOS Simulator 构建：通过。
- Release 主二进制扫描以下标识均为 0 命中：
  - `HOLO_MEMORY_SIMULATOR_SCENARIO`
  - `full-chain-v1`
  - `sim-validation-v1`
  - `HoloMemorySimulatorValidationScenario`
  - Mock 摘要与证据文案
- 场景仅在 `DEBUG + iOS Simulator + 显式环境变量` 三项同时成立时启用。
- 场景使用独立本地 SQLite，不接 CloudKit，不迁移真实记忆，不运行后台观察任务。

## 回归证据

- Feedback：20 assertions passed
- Forgetting/Tombstone：14 assertions passed
- Cross-domain Fusion：18 assertions passed
- Query Router/Service：28 assertions passed
- Simulator fixture/report：9 assertions passed
- 实际 App 场景：33 assertions passed

## 保留状态

以下内容必须保留到产品负责人明确确认“已看完，可清理”：

- 已安装并正在运行的 Holo Debug App
- `full-chain-v1` 隔离 Store
- `Documents/HoloMemoryValidation/full-chain-v1.json` 报告
- 当前财务记忆详情页面

禁止在确认前执行 uninstall、erase/reset simulator、删除 Store 或删除报告。
