# Progress: Holo Matter 战略收敛与首发重构

## 2026-09-21

- 读取 `planning-with-files` 技能说明。
- 发现项目根目录已有日历接入任务的规划文件，因此改用独立方案目录，避免覆盖。
- 回顾 Matter / Today 历史方案和当前工作区状态。
- 当前阶段：Phase 1，复核当前代码边界。
- 核对 Context Plan 卡、单项任务落库、Matter 激活与 Today 快照路径，确认清单归组、无效跳转、非原子创建和 Today 重复的结构性根因。
- 核对 Core Data 实体、Repository 事务边界、现有测试和 Prompt 真源；结论是数据模型可复用，但必须新建原子编排入口并收缩 Prompt。
- 完成 877 行主方案，包含产品宪法、首发范围、唯一用户旅程、信息架构、原子数据契约、Prompt 同步、文件范围、R0–R5、测试矩阵、发布门槛和回退。
- 完成结构检查：主文档包含编号 0–15 的 16 个主章节，代码围栏成对，目标路径无行尾空白。
- 本轮未修改业务代码，未运行构建、测试、模拟器、真机、CloudKit 或生产验证。
- 开始 Phase 5：针对“照方案做完仍然混乱”的反馈，将原来的旅程描述升级为一条产品、交互、数据和验收共用的唯一 Use Case。
- 完成 `UC-MATTER-001-国庆日本旅行从规划到完成完整用户UseCase.md`，覆盖 S0–S10 主旅程、INV-01–INV-10 数据红线、A1–A12 异常与恢复分支、完成退场以及 Gherkin 验收。
- 最后对账发现并修正两个硬伤：Links 实际为 10 条而非 9 条；当前没有持久化依赖图，因此首发改为持久化 `planOrder` 的线性计划。
- 已回写主方案：增加 `HoloMatterLink.planOrder`、固化 7 任务 / 10 Links 契约、取消首发 dependency graph，并对齐测试和 Definition of Done。
- 文档自检通过：Use Case 1186 行，主方案 905 行，代码围栏成对，无行尾空白，目标目录 `git diff --check` 通过。
- Phase 5 完成。本轮仍只修改方案文档，未修改业务代码，未运行构建、测试、模拟器、真机、CloudKit 或生产验证。
