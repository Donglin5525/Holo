# 跨模块真实旅程矩阵（G1 体验质量门槛）

> 建立：2026-09-16（G1）　依据：`docs/_common/plans/2026-09-16-Holo产品体验体检与三项优先工作完整实施方案.md` §2.2
> 用法：每条旅程实走后填「状态/证据/日期」。**「成功」一律以回读真实业务对象为准**；`Executed 0 tests`、`BUILD SUCCEEDED`、截图、日志单条都不能单独计为通过。
> 状态枚举：`未执行` / `模拟器过` / `模拟器部分`（注明断点）/ `真机过` / `失败`（注明问题编号 R4-x+）。
> 网络五态（正常/断网/弱网/超时/中途取消）适用于所有联网旅程（J1、J6、J7），每态须有明确终态、人话原因、可执行下一步；取消后旧任务不得复活。
> UI 矩阵（浅/深色、简/繁/英、小屏 iPhone SE 级、常规 iPhone、iPad 常见宽度、大字号 AX 档）适用于所有 UI 旅程；真实手感/系统权限/锁屏/小组件/CloudKit 一律真机，模拟器不代验。

## A. 主旅程矩阵（方案 §2.2 规定的最小集）

> **2026-09-16 G1 走查执行记录**：走查通道=QA-17Pro（iPhone 17 Pro / iOS 26.3.1）无头模拟器 + XCUITest（idb 不可用，走 XCUITest 既定后备通道）。新增走查套件 `G1JourneyWalkthroughUITests.swift`（已挂 pbxproj，未提交）。AX 标识探路产出：气泡 `firstStepBubble.main`、财务 FAB `finance.fab.addTransaction`、想法空态 `thoughtEmptyCta`/`plus`、想法卡菜单「更多操作」、记账表单（金额/支出收入/分类网格/账户行）。

| # | 旅程 | 起点 | 操作链 | 预期落库状态 | 可见反馈 | 失败恢复要求 | 设备 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|---|---|
| J1 | 全新安装首记 | 全新安装 | 卸载重装→三步引导「跳过引导」✓→首页第一步气泡出现 ✓→点气泡进 AI 页 ✓（输入框/发送按钮在）→气泡展示示范句、点击落焦不自动发送 ✓→财务 FAB 记账表单可开 ✓（金额/分类/账户元素齐） | Transaction 真实落库 | 发送后有记录成功态 | 断网发送失败有明确提示，不假成功 | 模拟器（表单交互/重启回读未走完）；真机未测 | 模拟器部分 | `testJ1FreshInstallBubbleAndAIExit` PASS（09-16）；记账数字键盘为自定义控件，XCUITest 键入未打通——落库断言由 ReceiptBookingKernelTests repository 层 30 断言（原子写/并发幂等/来源键）背书；气泡消失+庆祝+重启回读已在新用户激活 P0 二轮模拟器走查 PASS（2026-09-15，见 `docs/plans/2026-09-14-new-user-activation-p0-plan.md` 验收清单） |
| J2 | 想法全周期 | 想法模块 | 新建（空态 CTA/plus）✓→编辑器输入→防抖自动保存→重开回读→「更多操作」→删除→回收站口径确认→列表无残留 | Thought 软删+回收站批次 | 「…」菜单二次确认+「回收站保留30天」提示 | 新建带图不崩（09-16 双修回归点） | 模拟器；相册/iCloud 真机未测 | 模拟器（编辑器输入+自动保存已实证；删除链见 G1 套件运行结果） | run2：`testDumpModuleInterior` 证实编辑器 textView「想法内容」与键盘工具条输入文字；run5 修复 nav 重试后结果见 /tmp/qa_g1-uitests-all.log |
| J3 | 任务全周期 | 任务模块 | 新建→改日期→移回收件箱→完成→撤销→删除进回收站 | TodoTask 状态与 UI 一致 | 完成 3 秒撤回；回收站提示 | R1-6 nil 语义回归点 | 模拟器+真机未测 | 模拟器部分 | `testJ3TaskCreateAndReadback`（新建→输入→保存→列表回读），结果见 /tmp/qa_g1-uitests-all.log；改日期/移回收箱/完成撤销未单独走查（repository 层由 TodoTaskDatePolicy/TaskPostponePolicy 单测锁定） |
| J4 | 习惯打卡与撤销 | 习惯模块 | 打卡→列表撤销→看板撤销（确认弹窗）→统计即时更新 | HabitRecord 真实增删 | 打卡即时反馈；撤销确认 | 撤销后统计不残留 | 模拟器+真机未测 | 模拟器部分 | 本轮未覆盖打卡交互（自动化优先级让位 J1-J3）；等价证据=HabitRepository 单测（撤销 API+累计口径 8 断言）+ HabitStatsFloatUITests 既有走查；看板撤销 UI 于习惯撤销专项（765523a92/873343d95）QA 过 |
| J5 | Today/Matter 主行动 | 今天页+真实 Context Plan | 空态→方案→单项加入→激活→补链→主行动→完成后更新→续聊 | TodoTask 带 aiSource ID；MatterLink 真实 | 单项回执；已加入状态 | 补链失败可重试 | 模拟器+真机 | 模拟器（入口结构） | `TodayMatterVerticalSliceUITests.testTodayEntryAndPageStructure` 既有套件（本轮回归，见 uitests-all 日志）；日本旅行主验收=G2 旅程 B，本轮不替代 |
| J6 | 聊天与深度分析 | HoloAI | 问答→分析→取消→断网→重试→冷启动恢复 | 任务/消息持久化 | 流式；取消即停 | 409/额度终态不转圈 | 模拟器+真机 | 模拟器部分 | AI 页元素齐全已实证（run2 dump：发送/语音/图片识别/深度分析入口）；取消/断网/恢复属网络五态专项（本轮未测，见 C 表）；等价：HoloCloudAnalysisQAUITests 既有 2 测 |
| J7 | 长廊与日回放 | 记忆长廊 | 滚动→翻页→日回放→数据刷新 | 回放与真实记录一致 | 翻页不误触（ec37061b7 回归点） | 轴档不压死滚动 | 模拟器+真机 | 模拟器 | `CalendarTimelineSmokeUITests`（33 测）+ `CalendarPickerWalkUITests`（11 测）既有套件本轮回归，见 uitests-all 日志 |
| J8 | 系统集成 | 小组件+通知+深链 | 添加小组件→深链直达→权限拒绝→双设备 | 深链路由正确 | 权限拒绝人话引导 | 双设备不倒退 | 深链模拟器；小组件/通知/CloudKit 真机 | 模拟器（深链） | simctl openurl 深链路由验证（见执行记录）；小组件/通知/CloudKit 真机未测 |

### A2. 2026-09-16/17 G1 模拟器实走总结果（终版）

**通道**：QA-17Pro（有历史数据）+ Skip Audit（干净机+日历授权）两台 iPhone 17 Pro / iOS 26.3.1 无头模拟器；XCUITest（idb 不可用走既定后备）。

**主结果**：现有 27 个 UITest 套件 94 测：**74 过 / 18 败 / 2 跳过**。失败逐一定性：

| 失败 | 数量 | 判别 | 证据 |
|---|---|---|---|
| 健康 Phase2 走查 | 3 | 环境性：断言依赖 HealthKit 睡眠/步数种子数据，QA 干净模拟器无健康数据 | 失败文案「应有整晚睡眠时间轴卡」；该套件历史上在专用 seed 机通过 |
| 小红书摆拍截图 | 6 | 环境性：依赖摆拍 seed env 与专用截图机（Plus 态等） | 同套件 16 测过 |
| 云端分析基线 | 1 | 环境性：「聊天输入框未找到」需授权态 | 断言行 HoloCloudAnalysisQAUITests:270 |
| 日历总开关 B3 | 1 | 环境性→已判别：「显示的日历」行需 `isEnabled && 日历权限 fullAccess`；干净机 `simctl privacy grant calendar` 后 **PASS** | 复跑日志 /tmp/qa_g1-rerun-clean.log |
| Matter 焦点卡流程 | 1 | **测试过时（非功能回归）**：seed 本身成功（诊断日志实锤「matter created id=…」）；首页焦点卡按 T0-T5 设计退役（HomeView `matterFocusSection` 有 `!HoloTodayRolloutPolicy.isEnabled` 门），老测试（M0-M3 时代）断言首页卡已不成立；新版语义由 TodayMatterVerticalSliceUITests 覆盖且 PASS | /tmp/qa_g1-final-uitest.log + 沙盒诊断 matter-seed-diag.log |
| 想法图片来源 sheet | 1 | 测试基建：`TextView isHittable` 误报（frame 两机不同但同样 not hittable）——App 常驻 ZStack 浮层结构与 XCUITest hittable 算法系统性不兼容；功能正常（同位置 typeText 实际输入成功） | 两台机同失败形态 |
| G1 走查（本轮新增） | J2×2+J3 断言 | 同上 hittable/键盘焦点合成事件限制；脚本 3 轮迭代后按 self-check 纪律停手，J1 表单可达断言 PASS | /tmp/qa_g1-final-uitest.log |

**过时/基建类修复建议**（待东林拍板，涉及验收口径）：① MatterVerticalSliceUITests 断言迁「今天」页或退役；② 常驻 ZStack 下 XCUITest hittable 误报的长期对策（改 identifier 直查+坐标点击范式，或对浮层 App 引入 XCUITest 专用 accessibility 快照模式）。

**J1-J8 对应结论**：J1 表单可达+气泡链已验（记账数字键盘自动化未打通，落库由 repository 层 30 断言+新用户激活 P0 走查背书）；J2 输入/自动保存实证+删除流由单测锁定；J3/J4 交互自动化未走完（单测锁定+既有套件部分覆盖）；J5 Today 入口结构 PASS；J6 AI 页元素齐全；J7 日历/长廊 smoke PASS；J8 深链为通知驱动需真机。


### A3. G2 旅程验收执行记录（2026-09-17 上午）

**通道限制实锤（2026-09-17 与并行会话交叉后修正定性）**：新装态「点击无反应/页面假死」的真因有二：① **全新安装首启 CoreDataStack 三方锁死锁（P0，并行会话已定位、未修待拍板）**——主线程假死导致 AX 事件随机失效（快照可读），模拟器必现卡启动页；QA 绕过 key=`thoughtSemanticV3.topicLinkBackfillDone`。② **ChatView 巨型内联视图树栈贴线（并行会话已根治、未提交待东林真机复验）**——模拟器不崩但渲染极慢/失败=「AI 页打不开」，真机直接闪退（4 份 .ips 实锤）。在此之上，无头模拟器 AX 事件派发本身也有随机失效成分。**结论：AX 快照读取可靠、事件派发不可依赖；真机闪退类（栈余量型）模拟器复现不了，勿在模拟器空耗。**

**旅程 A（全新用户）已自动化拿到的证据**：
| 断言 | 结果 |
|---|---|
| 全新安装 → 引导可跳过 | PASS |
| 空库首页出现第一步气泡 | PASS |
| 气泡关闭后立即消失 | PASS（坐标点击后） |
| 关闭持久化：重启不复现 | PASS |
| 记账表单入口（空态 CTA/FAB 两态）与表单元素 | PASS |
| 记账落库回读/庆祝/气泡消失 | **转人工**（数字键盘输入受通道随机性影响未完成；落库逻辑由 ReceiptBookingKernelTests 30 断言 + 新用户激活 P0 人工二轮走查 09-15 PASS 背书） |
| AI 一句话记账（真实后端） | **转人工/真机**（同通道限制） |
| 老用户（有数据）无气泡 | PASS（重启有数据态断言） |
| 次日行动出口（真实跨天） | **转真机**（模拟器无法跨天） |

**旅程 B（日本旅行）**：依赖连续多步 AI 实时交互点击，通道随机失效下不可自动化，**整体转真机人工验收**（清单见下）。Context Plan/Matter 后端与数据链路由单测锁定（ContextPlan/Matter 系套件）。

**首启死锁 P0 修复验证（2026-09-17，6644e776f）**：① 三轮全新安装（不写绕过 key）全部 12 秒内完成启动+topicLink 回填（修复前必现卡启动页）；② 首装首启 UI 到首页（气泡断言过）+ 关闭持久化 PASS；③ 全量单测 1131/2（唯一失败=Agent 域锁屏测试，HEAD 上确定性失败，属并行域已移交，见台账 R4-3 遗留）；④ 五大入口走查：今天/财务项目/账户验收/长廊日历全 PASS，4 败均为已知定性（空库断言跑在有数据态 ×2、通道 hittable ×2）。**新装用户卡启动页的阻塞已解除。**

**真机人工验收清单（东林 ~10 分钟）**：
1. 全新安装 → 跳过引导 → 点气泡 → 发「午饭花了35元」→ 确认 AI 回执+财务出现该笔 → 回首页看庆祝+气泡消失
2. AI 发「国庆准备去日本，签证、猫、日元和攻略怎么安排？」→ 方案卡单项「加入待办」→「开始整理」→ 今天页主行动 → 完成任务看主行动变化 → Matter 内说「酒店订好了」→ 重启验证状态
3. 断网发一句 → 确认失败提示人话可重试

## B. 定向回归（近期新增改动，2026-09-16 G1 追加）

| # | 对象 | 回归点 | 关联在途改动 | 状态 | 证据 |
|---|---|---|---|---|---|
| D1 | 想法图片链路 | 新建+选图不崩（转正在途标志）；iCloud 探测先行四级链路；失败文案细分；任务/反馈/AI 选图共用链路 | PhotoLibraryImageLoader 重构（09-16 晚，未提交） | 未执行 | |
| D2 | iPad 可用性 0-2 | holoContentWidth 契约：iPhone 零回归（恒 nil）；侧边栏 232/60 两态不重建业务状态；财务右栏详情；AI 报告侧栏 | HoloLayoutPolicy 等（09-16 晚，未提交） | 未执行 | |
| D3 | 记忆低确认成本 P0-P6 | 收件箱三处下线；五路决策 21 fixtures；澄清≤1问/30天冷却；「不再使用」墓碑不复活；迁移不动用户决定 | Memory 批（09-16 晨，未提交） | 未执行 | |
| D4 | 云端叙事 P0 | narrativeSummary/keyInsight/interpretation 端到端保真；旧结果降级；「发现 N」退役 | Cloud 批（09-15，iOS 未提交；后端已发版 18871f06f） | 未执行 | |
| D5 | 新用户激活 P0 | 气泡只出现一次；旧用户不可见；预填不自动发送；首记庆祝一次 | NewUserActivationState 等（09-15，未提交） | 未执行 | |
| D6 | 边缘右滑 | 让位判定沿响应链；睡眠详情/个人页右滑正常；禁用态穿透 | SwipeBackModifier（09-16 晨） | 未执行 | |
| D7 | Today/Matter T0-T5 已提交代码 | 焦点确定性排序（stale/suggested 不当主行动）；单项加入幂等；补链双向 | 1ec53a4b7（已提交）+ 后续在途 | 未执行 | |

## C. 网络五态 × 联网旅程

| 旅程 | 正常 | 断网 | 弱网 | 超时 | 中途取消 |
|---|---|---|---|---|---|
| J1 AI 一句话记账 | | | | | |
| J6 普通回答 | | | | | |
| J6 深度分析（云端） | | | | | |
| J7 回放生成 | | | | | |
| 想法离线整理 | | | | | |
| 记忆萃取 | | | | | |

## D. 执行纪律

1. 每格填日期+设备+证据路径；模拟器截图存 `/tmp/qa-g1/`（会话内）并择要归档 `docs/qa-10-rounds/evidence/`。
2. 发现问题记入 ledger.md 对应轮次小节（第 4 轮起），编号沿用 R{轮次}-{序号}。
3. P0 发现即修即回归；P1 本轮清零；P2 记录影响与时机，设计类先出方案给东林。
4. 修第二个同类问题时必须检查是否统一规则/共享组件，不逐页复制修补。
