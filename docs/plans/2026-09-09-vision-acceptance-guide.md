# 截图识别记账 · 明早验收指南（2026-09-09 通宵交付）

> 给东林的验收动线。后端已上生产，iOS 已完工并**完成无头模拟器端到端走查（四场景全过）**。

## 〇、E2E 走查结果（2026-09-09 上午，iPhone 17 模拟器·繁中环境·生产后端）

| 场景 | 结果 |
|---|---|
| A 小票记账 | ✅ 相册选瑞幸小票 → 缩略图气泡 +「正在看圖…」可取消 → 确认卡 ¥19.90/条目摘要/8月28日 → 确认落账 |
| A2 账户归位 | ✅ 交易自动记到「微信」账户（未落默认現金）——拍板 6 生效实证 |
| C 转账拦截 | ✅ 微信转账 ¥500 截图 →「這是轉帳/還款類截圖，屬於資金流轉，我不把它記成消費。」零误记 |
| 外币拒识 | ✅ 美元小票 →「這是外幣消費，目前只支持人民幣記賬…」 |
| 防重提示 | ✅ 同一小票重发 →「提醒：這筆可能已經記過（2026/8/28 ¥19.90）…」且不阻断（走查中修了一个真缺陷：窗口原按交易日期比对，票面旧日期连拍会漏报，已改按创建时间） |
| 单测 | ✅ VisionStandaloneTests 9/9（走查中还揪出压缩管线按「点」降采样、scale3 屏实际 7200px 的问题，已改像素空间显式降采样到 2000px） |

走查截图存 /tmp/vision-e2e-*.png（模拟器临时区）。

## 一、今晚交付了什么（一句话版）

小票/支付截图拍一张发到聊天里，Holo 认出金额、商户、日期、支付方式，弹记账确认卡，确认后自动记到匹配的账户；转账截图、外币小票、没付款的订单会诚实拒绝，不乱记账。

## 二、上线状态

| 端 | 状态 |
|---|---|
| 后端 `/v1/ai/vision/extract` | **已发版生产**（commit c9473352f，验收+烟雾测试四场景全过：小票金额/商户/日期/通道全对、美元小票拒识、转账拦截、多品小票正常，单次 3-6 秒） |
| iOS 聊天识别链路 | 代码完工，主 App 编译通过；**端到端真机/模拟器验收待做**（见「阻塞」） |
| 模型 | 默认 qwen3-vl-plus（M0 五轮评测 23-24/24），**换模型=改一行 env 重启，不用改代码** |

**阻塞说明（已解除）**：夜间整包编译曾被并行会话的 Widgets 工程在途缺口卡住，其收敛后恢复；E2E 走查已完成（见 §〇）。真机验收仍建议走一遍（§三），多验真机实拍小票的真实鲁棒性。

## 三、真机验收动线（建议 10 分钟）

1. Xcode 选真机 Run（开发证书包装到手机，走 CloudKit Development 环境不影响本功能——本功能不依赖 CloudKit）。
2. 进「对话」页 → 输入栏左侧新增的 **图片按钮** → 「拍照」或「从相册选择」。
3. **场景 A 正常记账**：拍一张真实小票/支付截图（可先在输入框打附言再选图）→ 出现「正在看图…」→ 出确认卡 → 核对金额/分类/日期 → 确认 → 去财务页看交易落账。
4. **场景 B 微信/支付宝识别**：用一张微信支付截图 → 确认后看这笔钱记没记到名字带「微信」的账户（没有该账户则落默认账户）。
5. **场景 C 转账拦截**：发一张微信转账截图 → 应收到拒识说明，不出确认卡。
6. **场景 D 防重**：同一张小票发两次 → 第二次应在确认前提示「可能已记过」。
7. 顺手看：聊天历史里自己发的图有缩略图；识别中点停止按钮能取消。

## 四、模型决策备忘录

**当前生产已切 DeepSeek（2026-09-09 东林拍板）：deepseek-v4-flash-vision-exp**，走独立钥匙通道（deepseek-vision provider，与主聊天 DeepSeek 钥匙隔离）；**思考模式已关**（REASONING_EFFORT=none，东林拍板「识别不需要思考」——实测关后快 2-3 倍且质量反升）；maxTokens 4000 兜底。烟雾+复测全过。

| 模型/档位 | 24 张评测 | 单均 tokens | 特点 |
|---|---|---|---|
| **deepseek-v4-flash-vision-exp·关思考（在用）** | **24/24 满分**，拦截 4/4 | **~1000（通义 1/3）** | 典型 3-6s；偶发慢=并发撞 5 次/分限流桶排队，单用户无感 |
| deepseek 同款·开思考 | 22-23/24 + 一次空回复 | ~1100+思考开销 | 已弃用 |
| qwen3-vl-plus（原默认） | 23-24/24，拦截 4/4 | ~3240 | 备选；env 一行切回 |

两档安全红线（转账/外币拦截）都是满分，日常小票识别都在水准上——**差异主要在成本与偶发延迟**，DeepSeek 是通义三分之一的价格。想切回：ECS `.env.production` 改 `HOLO_VISION_EXTRACTION_PROVIDER=qwen`（或删掉该行走默认）+ 删 `HOLO_VISION_EXTRACTION_MODEL`，重启即生效。

**成本口径**（拍板 3 遗留提醒已立项）：上线后按 ai_call_logs 按 purpose=vision_extraction 聚合实测，主动向东林汇报再定额度策略。当前每设备每天上限 20 次（防滥用桶），不占会员额度。

## 五、已知边界（按拍板执行，非遗漏）

- 外币拒识（拍板 7）；转账/还款/理财拦截（防虚增支出）；清单截图暂不支持转任务（二期）。
- 识别置信度低（<0.45）不出卡，引导重拍或手填——宁可问不瞎猜。
- 同一笔小票重复发送是「提示」不是「阻断」（拍板 9）。
- 聊天图片缩略图只存本机（换设备看图不跟随同步，属预期）；CloudKit 字段化留待 1.0.3 schema 增量一起做。
- 隐私：图片压缩剥位置信息后上传，**识别完即弃**，不落盘不进日志正文（日志只有元数据，代码层强制）。

## 六、遗留清单（不阻塞验收）

1. ~~无头模拟器四场景走查~~ ✅ 已完成（§〇）；真机实拍小票的鲁棒性验证待东林真机验收时回灌。
2. ~~VisionStandaloneTests~~ ✅ 9/9 绿。
3. xcstrings 三语词表 +384 行在工作区（与在途多语言改动同文件，提交时随多语言一起入库）。
4. 隐私政策补「图片识别」披露（方案 §8.1，1.0.3 提审前必须做）。
5. 聊天图片缩略图的 CloudKit 字段化（跨设备看图）留待 1.0.3 schema 增量一起做。
