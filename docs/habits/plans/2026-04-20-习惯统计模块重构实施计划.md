# Holo 习惯统计模块重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构 Holo 习惯统计模块，将其改造成“当前自然月 + 周视图优先 + 单开展开月历 + 设置管理展示与排序”的高频浏览面板。

**Architecture:** 保留 `HabitRepository` 作为习惯记录数据源，但新增“按自然月投影”的统计模型，让 UI 直接消费周/月视图数据，而不是在 SwiftUI 中临时拼装。用单页滚动仪表板替换现有双 Tab 统计页，并增加一层轻量持久化配置来保存统计页展示习惯与排序。

**Tech Stack:** SwiftUI, Core Data, XCTest, xcodebuild, existing `HabitRepository` / `HabitStatsState` patterns

---

## File Structure

### Existing files to modify

- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/HabitsView.swift`
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsView.swift`
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsState.swift`
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitRepository.swift`

### New files to create

- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsDisplaySettings.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsSettingsView.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsSummaryCard.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsMonthSwitcher.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitWeekStripView.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitMonthGridView.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsExpandableCardView.swift`

### Existing files to delete after migration compiles

- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsOverviewTab.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsHabitsTab.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitOverviewCard.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitRankingCard.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitTimeRangeSelector.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitTrendChartView.swift`

### Tests to add

- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/HoloTests/Models/HabitStatsStateTests.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/HoloTests/Models/HabitStatsDisplaySettingsTests.swift`

---

### Task 1: Build Month-Scoped Data Projections

**Files:**
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitRepository.swift`
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsState.swift`
- Test: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/HoloTests/Models/HabitStatsStateTests.swift`

- [ ] **Step 1: Write the failing month/week projection tests**

```swift
import XCTest
@testable import Holo

final class HabitStatsStateTests: XCTestCase {

    func testCollapsedWeekFallsBackToLastRecordedWeekInsideMonth() {
        let calendar = Calendar(identifier: .gregorian)
        let month = calendar.date(from: DateComponents(year: 2026, month: 4, day: 1))!
        let firstWeek = HabitStatsWeekSlice(
            weekStart: calendar.date(from: DateComponents(year: 2026, month: 4, day: 21))!,
            days: []
        )
        let secondWeek = HabitStatsWeekSlice(
            weekStart: calendar.date(from: DateComponents(year: 2026, month: 4, day: 28))!,
            days: []
        )
        let item = HabitStatsDisplayItem(
            habitId: UUID(),
            name: "体重",
            icon: "scalemass",
            habitColorHex: "#3B82F6",
            type: .measure,
            summary: .measure(recordedDays: 18, averageValueText: "58.2kg"),
            collapsedWeek: secondWeek,
            allWeeks: [firstWeek, secondWeek],
            month: HabitStatsMonthSection(monthStart: month, weekdaySymbols: [], rows: [])
        )

        XCTAssertEqual(item.collapsedWeek.weekStart, secondWeek.weekStart)
    }

    func testMonthChangeCollapsesExpandedHabit() async {
        let state = HabitStatsState(repository: .previewStub)
        let originalHabitId = UUID()
        state.expandedHabitId = originalHabitId

        await state.selectMonth(Date(timeIntervalSince1970: 1_775_000_000))

        XCTAssertNil(state.expandedHabitId)
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
xcodebuild test \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:HoloTests/HabitStatsStateTests
```

Expected: FAIL because `HabitStatsWeekSlice`, `HabitStatsDisplayItem`, `HabitStatsMonthSection`, and the injectable `HabitStatsState` initializer do not exist yet.

- [ ] **Step 3: Add month/week projection types to `HabitStatsState.swift`**

```swift
enum HabitStatsCardKind: Equatable {
    case checkIn
    case count
    case measure
}

struct HabitStatsDayCell: Identifiable, Equatable {
    let date: Date
    let dayNumber: Int?
    let isInCurrentMonth: Bool
    let isToday: Bool
    let hasRecord: Bool
    var id: Date { date }
}

struct HabitStatsWeekSlice: Equatable {
    let weekStart: Date
    let days: [HabitStatsDayCell]
}

struct HabitStatsMonthSection: Equatable {
    let monthStart: Date
    let weekdaySymbols: [String]
    let rows: [[HabitStatsDayCell]]
}

enum HabitStatsCardSummary: Equatable {
    case checkIn(completedDays: Int, streak: Int)
    case count(recordedDays: Int, totalCountText: String)
    case measure(recordedDays: Int, averageValueText: String)
}

struct HabitStatsDisplayItem: Identifiable, Equatable {
    let habitId: UUID
    let name: String
    let icon: String
    let habitColorHex: String
    let type: HabitStatsCardKind
    let summary: HabitStatsCardSummary
    let collapsedWeek: HabitStatsWeekSlice
    let allWeeks: [HabitStatsWeekSlice]
    let month: HabitStatsMonthSection
    var id: UUID { habitId }
}
```

- [ ] **Step 4: Add repository helpers that build the display models**

```swift
func getHabitStatsDisplayItems(
    month: Date,
    visibleHabitIds: [UUID]?,
    orderedHabitIds: [UUID]?
) -> [HabitStatsDisplayItem] {
    let monthStart = Calendar.current.date(from: Calendar.current.dateComponents([.year, .month], from: month))!
    let monthEnd = Calendar.current.date(byAdding: DateComponents(month: 1, day: -1), to: monthStart)!
    let habits = orderedHabits(visibleHabitIds: visibleHabitIds, orderedHabitIds: orderedHabitIds)

    return habits.map { habit in
        let monthCells = makeMonthCells(for: habit, monthStart: monthStart, monthEnd: monthEnd)
        let weeks = makeWeekSlices(from: monthCells)
        let collapsedWeek = weeks.last(where: { $0.days.contains(where: \.hasRecord) }) ?? weeks.first!

        return HabitStatsDisplayItem(
            habitId: habit.id,
            name: habit.name,
            icon: habit.icon,
            habitColorHex: habit.color,
            type: statsCardKind(for: habit),
            summary: statsSummary(for: habit, monthStart: monthStart, monthEnd: monthEnd),
            collapsedWeek: collapsedWeek,
            allWeeks: weeks,
            month: HabitStatsMonthSection(
                monthStart: monthStart,
                weekdaySymbols: shortWeekdaySymbols(),
                rows: stride(from: 0, to: monthCells.count, by: 7).map { Array(monthCells[$0..<min($0 + 7, monthCells.count)]) }
            )
        )
    }
}

private func orderedHabits(visibleHabitIds: [UUID]?, orderedHabitIds: [UUID]?) -> [Habit] {
    let visible = Set(visibleHabitIds ?? activeHabits.map(\.id))
    let filtered = activeHabits.filter { visible.contains($0.id) }
    let order = Dictionary(uniqueKeysWithValues: (orderedHabitIds ?? []).enumerated().map { ($1, $0) })
    return filtered.sorted { (order[$0.id] ?? .max) < (order[$1.id] ?? .max) }
}
```

- [ ] **Step 5: Update `HabitStatsState` to load month-scoped cards**

```swift
@MainActor
final class HabitStatsState: ObservableObject {
    @Published var selectedMonth: Date = Date()
    @Published var expandedHabitId: UUID?
    @Published var summaryStats: HabitOverviewStats = .empty()
    @Published var displayItems: [HabitStatsDisplayItem] = []
    @Published var hasAnyHabits: Bool = false

    private let repository: HabitRepository
    private let displaySettings: HabitStatsDisplaySettings

    init(
        repository: HabitRepository = .shared,
        displaySettings: HabitStatsDisplaySettings = .shared
    ) {
        self.repository = repository
        self.displaySettings = displaySettings
        Task { await reload() }
    }

    func selectMonth(_ month: Date) async {
        selectedMonth = month
        expandedHabitId = nil
        await reload()
    }

    func reload() async {
        hasAnyHabits = !repository.activeHabits.isEmpty
        summaryStats = repository.getOverviewStats(forMonth: selectedMonth, visibleHabitIds: displaySettings.visibleHabitIds)
        displayItems = repository.getHabitStatsDisplayItems(
            month: selectedMonth,
            visibleHabitIds: displaySettings.visibleHabitIds,
            orderedHabitIds: displaySettings.orderedHabitIds
        )
    }

    func toggleExpansion(for habitId: UUID) {
        if expandedHabitId == habitId {
            expandedHabitId = nil
        } else {
            expandedHabitId = habitId
        }
    }
}
```

- [ ] **Step 6: Run test to verify it passes**

Run:

```bash
xcodebuild test \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:HoloTests/HabitStatsStateTests
```

Expected: PASS with `HabitStatsStateTests` green.

- [ ] **Step 7: Commit**

```bash
git add \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitRepository.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsState.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/HoloTests/Models/HabitStatsStateTests.swift"
git commit -m "feat(habits): add month-scoped stats projections"
```

### Task 2: Persist Visible Habits and Manual Order

**Files:**
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsDisplaySettings.swift`
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsState.swift`
- Test: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/HoloTests/Models/HabitStatsDisplaySettingsTests.swift`

- [ ] **Step 1: Write the failing settings persistence tests**

```swift
import XCTest
@testable import Holo

final class HabitStatsDisplaySettingsTests: XCTestCase {

    func testSaveVisibleHabitIdsRoundTrips() {
        let defaults = UserDefaults(suiteName: "HabitStatsDisplaySettingsTests")!
        defaults.removePersistentDomain(forName: "HabitStatsDisplaySettingsTests")
        let first = UUID()
        let second = UUID()
        let settings = HabitStatsDisplaySettings(userDefaults: defaults)

        settings.setVisibleHabitIds([first, second])

        XCTAssertEqual(settings.visibleHabitIds, [first, second])
    }

    func testMoveHabitReordersIds() {
        let defaults = UserDefaults(suiteName: "HabitStatsDisplaySettingsMoveTests")!
        defaults.removePersistentDomain(forName: "HabitStatsDisplaySettingsMoveTests")
        let a = UUID()
        let b = UUID()
        let c = UUID()
        let settings = HabitStatsDisplaySettings(userDefaults: defaults)
        settings.setOrderedHabitIds([a, b, c])

        settings.moveHabit(fromOffsets: IndexSet(integer: 0), toOffset: 3)

        XCTAssertEqual(settings.orderedHabitIds, [b, c, a])
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
xcodebuild test \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:HoloTests/HabitStatsDisplaySettingsTests
```

Expected: FAIL because `HabitStatsDisplaySettings` does not exist.

- [ ] **Step 3: Create the persistence model**

```swift
import Foundation

@MainActor
final class HabitStatsDisplaySettings: ObservableObject {
    static let shared = HabitStatsDisplaySettings()

    @Published private(set) var visibleHabitIds: [UUID]
    @Published private(set) var orderedHabitIds: [UUID]

    private let userDefaults: UserDefaults
    private let visibleKey = "habit.stats.visible.ids"
    private let orderKey = "habit.stats.order.ids"

    init(userDefaults: UserDefaults = .standard) {
        self.userDefaults = userDefaults
        self.visibleHabitIds = Self.loadUUIDs(forKey: visibleKey, from: userDefaults)
        self.orderedHabitIds = Self.loadUUIDs(forKey: orderKey, from: userDefaults)
    }

    func setVisibleHabitIds(_ ids: [UUID]) {
        visibleHabitIds = ids
        save(ids, forKey: visibleKey)
    }

    func setOrderedHabitIds(_ ids: [UUID]) {
        orderedHabitIds = ids
        save(ids, forKey: orderKey)
    }

    func moveHabit(fromOffsets: IndexSet, toOffset: Int) {
        var copy = orderedHabitIds
        copy.move(fromOffsets: fromOffsets, toOffset: toOffset)
        setOrderedHabitIds(copy)
    }

    private func save(_ ids: [UUID], forKey key: String) {
        userDefaults.set(ids.map(\.uuidString), forKey: key)
    }

    private static func loadUUIDs(forKey key: String, from defaults: UserDefaults) -> [UUID] {
        (defaults.stringArray(forKey: key) ?? []).compactMap(UUID.init(uuidString:))
    }
}
```

- [ ] **Step 4: Wire settings reloads into `HabitStatsState`**

```swift
private var cancellables: Set<AnyCancellable> = []

private func bindDisplaySettings() {
    displaySettings.$visibleHabitIds
        .combineLatest(displaySettings.$orderedHabitIds)
        .sink { [weak self] _, _ in
            Task { await self?.reload() }
        }
        .store(in: &cancellables)
}
```

- [ ] **Step 5: Run test to verify it passes**

Run:

```bash
xcodebuild test \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:HoloTests/HabitStatsDisplaySettingsTests \
  -only-testing:HoloTests/HabitStatsStateTests
```

Expected: PASS for both test classes.

- [ ] **Step 6: Commit**

```bash
git add \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsDisplaySettings.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsState.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/HoloTests/Models/HabitStatsDisplaySettingsTests.swift"
git commit -m "feat(habits): persist stats visibility and order"
```

### Task 3: Build Week/Month Card Components

**Files:**
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsSummaryCard.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsMonthSwitcher.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitWeekStripView.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitMonthGridView.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsExpandableCardView.swift`

- [ ] **Step 1: Create the month switcher and summary card**

```swift
struct HabitStatsMonthSwitcher: View {
    let month: Date
    let onTap: () -> Void

    var body: some View {
        Button(action: onTap) {
            HStack {
                VStack(alignment: .leading, spacing: 4) {
                    Text("当前自然月")
                        .font(.holoLabel)
                        .foregroundColor(.holoTextSecondary)
                    Text(month.formatted(.dateTime.year().month(.wide)))
                        .font(.holoHeading)
                        .foregroundColor(.holoTextPrimary)
                }
                Spacer()
                Text("点击切换月份")
                    .font(.holoLabel)
                    .foregroundColor(.holoPrimary)
                    .padding(.horizontal, 10)
                    .padding(.vertical, 6)
                    .background(Color.holoPrimary.opacity(0.12))
                    .clipShape(Capsule())
            }
        }
        .buttonStyle(.plain)
    }
}

struct HabitStatsSummaryCard: View {
    let stats: HabitOverviewStats

    var body: some View {
        VStack(alignment: .leading, spacing: HoloSpacing.md) {
            Text("月度总览")
                .font(.holoCaption)
                .foregroundColor(.holoTextSecondary)
            Text("\(Int(stats.todayCompletionRate.rounded()))% 保持节奏")
                .font(.holoHeading)
                .foregroundColor(.holoTextPrimary)
            HStack(spacing: HoloSpacing.sm) {
                summaryPill("展示中", "\(stats.totalHabits)")
                summaryPill("完成率", "\(Int(stats.averageCompletionRate.rounded()))%")
                summaryPill("最佳连续", "\(stats.totalStreak)天")
            }
        }
        .padding(HoloSpacing.md)
        .background(Color.holoCardBackground)
        .clipShape(RoundedRectangle(cornerRadius: HoloRadius.lg))
    }
}
```

- [ ] **Step 2: Create the collapsed week strip and expanded month grid**

```swift
struct HabitWeekStripView: View {
    let week: HabitStatsWeekSlice
    let accentColor: Color

    var body: some View {
        HStack(spacing: 6) {
            ForEach(week.days) { day in
                RoundedRectangle(cornerRadius: 8)
                    .fill(day.isToday ? Color.holoPrimary.opacity(0.18) : Color.holoBackground)
                    .overlay(alignment: .bottomTrailing) {
                        if day.hasRecord {
                            Circle()
                                .fill(accentColor)
                                .frame(width: 6, height: 6)
                                .padding(4)
                        }
                    }
                    .aspectRatio(1, contentMode: .fit)
            }
        }
    }
}

struct HabitMonthGridView: View {
    let month: HabitStatsMonthSection
    let accentColor: Color

    var body: some View {
        VStack(spacing: 6) {
            HStack {
                ForEach(month.weekdaySymbols, id: \.self) { symbol in
                    Text(symbol)
                        .font(.holoTinyLabel)
                        .foregroundColor(.holoTextSecondary)
                        .frame(maxWidth: .infinity)
                }
            }
            ForEach(Array(month.rows.enumerated()), id: \.offset) { _, row in
                HStack(spacing: 6) {
                    ForEach(row) { day in
                        dayCell(day)
                    }
                }
            }
        }
    }

    private func dayCell(_ day: HabitStatsDayCell) -> some View {
        RoundedRectangle(cornerRadius: 8)
            .fill(day.isInCurrentMonth ? Color.holoBackground : Color.holoBackground.opacity(0.4))
            .overlay(alignment: .topLeading) {
                if let number = day.dayNumber {
                    Text("\(number)")
                        .font(.system(size: 10, weight: .medium))
                        .foregroundColor(day.isInCurrentMonth ? .holoTextPrimary : .holoTextSecondary)
                        .padding(4)
                }
            }
            .overlay(alignment: .bottomTrailing) {
                if day.hasRecord {
                    Circle()
                        .fill(accentColor)
                        .frame(width: 6, height: 6)
                        .padding(4)
                }
            }
            .aspectRatio(1, contentMode: .fit)
    }
}
```

- [ ] **Step 3: Create the expandable card view**

```swift
struct HabitStatsExpandableCardView: View {
    let item: HabitStatsDisplayItem
    let isExpanded: Bool
    let onTap: () -> Void

    var body: some View {
        let accent = Color(hex: item.habitColorHex)

        VStack(alignment: .leading, spacing: HoloSpacing.md) {
            header(accent: accent)
            if isExpanded {
                HabitMonthGridView(month: item.month, accentColor: accent)
                    .transition(.opacity.combined(with: .move(edge: .top)))
            } else {
                HabitWeekStripView(week: item.collapsedWeek, accentColor: accent)
                    .transition(.opacity)
            }
        }
        .padding(HoloSpacing.md)
        .background(Color.holoCardBackground)
        .clipShape(RoundedRectangle(cornerRadius: HoloRadius.lg))
        .contentShape(Rectangle())
        .onTapGesture {
            withAnimation(.easeInOut(duration: 0.24)) {
                onTap()
            }
        }
    }
}
```

- [ ] **Step 4: Add SwiftUI previews for the new components**

```swift
#Preview("Expanded Card") {
    HabitStatsExpandableCardView(
        item: HabitStatsDisplayItem(
            habitId: UUID(),
            name: "健身",
            icon: "figure.strengthtraining.traditional",
            habitColorHex: "#F46D38",
            type: .count,
            summary: .count(recordedDays: 12, totalCountText: "12次"),
            collapsedWeek: HabitStatsWeekSlice(weekStart: .now, days: []),
            allWeeks: [],
            month: HabitStatsMonthSection(monthStart: .now, weekdaySymbols: [], rows: [])
        ),
        isExpanded: true,
        onTap: {}
    )
    .padding()
    .background(Color.holoBackground)
}
```

- [ ] **Step 5: Build the app to verify the new components compile**

Run:

```bash
xcodebuild build \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
```

Expected: BUILD SUCCEEDED with the new components compiling and previews resolving.

- [ ] **Step 6: Commit**

```bash
git add \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsSummaryCard.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsMonthSwitcher.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitWeekStripView.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitMonthGridView.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitStatsExpandableCardView.swift"
git commit -m "feat(habits): add expandable stats card components"
```

### Task 4: Rewrite the Stats Dashboard Screen

**Files:**
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsView.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsOverviewTab.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsHabitsTab.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitOverviewCard.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitRankingCard.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitTimeRangeSelector.swift`
- Delete: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitTrendChartView.swift`

- [ ] **Step 1: Replace the old dual-tab layout with the dashboard skeleton**

```swift
struct HabitStatsView: View {
    let onBack: () -> Void
    @StateObject private var state = HabitStatsState()
    @State private var isMonthPickerPresented = false

    var body: some View {
        VStack(spacing: 0) {
            navigationBar
            ScrollView(showsIndicators: false) {
                VStack(spacing: HoloSpacing.md) {
                    HabitStatsMonthSwitcher(month: state.selectedMonth) {
                        isMonthPickerPresented = true
                    }
                    HabitStatsSummaryCard(stats: state.summaryStats)
                    cardsList
                }
                .padding(.horizontal, HoloSpacing.md)
                .padding(.bottom, HoloSpacing.xl)
            }
        }
        .background(Color.holoBackground)
    }
}
```

- [ ] **Step 2: Add the single-expand card list**

```swift
private var cardsList: some View {
    LazyVStack(spacing: HoloSpacing.sm) {
        ForEach(state.displayItems) { item in
            HabitStatsExpandableCardView(
                item: item,
                isExpanded: state.expandedHabitId == item.habitId
            ) {
                state.toggleExpansion(for: item.habitId)
            }
        }
    }
}
```

- [ ] **Step 3: Add the month picker hook**

```swift
.sheet(isPresented: $isMonthPickerPresented) {
    let year = Calendar.current.component(.year, from: state.selectedMonth)
    let month = Calendar.current.component(.month, from: state.selectedMonth)
    MonthYearPickerView(
        currentYear: year,
        currentMonth: month,
        onConfirm: { selectedYear, selectedMonth in
            let nextMonth = Calendar.current.date(
                from: DateComponents(year: selectedYear, month: selectedMonth, day: 1)
            ) ?? state.selectedMonth
            Task { await state.selectMonth(nextMonth) }
            isMonthPickerPresented = false
        },
        onCancel: {
            isMonthPickerPresented = false
        }
    )
    .presentationDetents([.height(320)])
}
```

- [ ] **Step 4: Remove dead stats-dashboard files once the new screen builds**

```bash
rm \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsOverviewTab.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsHabitsTab.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitOverviewCard.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitRankingCard.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitTimeRangeSelector.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/Components/HabitTrendChartView.swift"
```

- [ ] **Step 5: Run full unit tests and a build check**

Run:

```bash
xcodebuild test \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
```

Expected: PASS for the existing AI/component tests plus the new stats tests.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(habits): replace stats tabs with dashboard layout"
```

### Task 5: Add the Stats Settings Tab and Move Add Action Into Habits

**Files:**
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/HabitsView.swift`
- Create: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsSettingsView.swift`

- [ ] **Step 1: Change the bottom tab enum from `新增` to `设置`**

```swift
enum HabitTab: String, CaseIterable {
    case stats = "统计"
    case habits = "习惯"
    case settings = "设置"

    var icon: String {
        switch self {
        case .stats: return "chart.bar.fill"
        case .habits: return "checkmark.circle.fill"
        case .settings: return "gearshape.fill"
        }
    }
}
```

- [ ] **Step 2: Route the third tab to the settings screen**

```swift
switch selectedTab {
case .stats:
    HabitStatsView(onBack: { dismiss() })
case .habits:
    HabitListView(onBack: { dismiss() }, showAddHabit: $showAddHabit)
case .settings:
    HabitStatsSettingsView(onBack: { dismiss() })
}
```

- [ ] **Step 3: Add an in-list add button to the habits tab header**

```swift
private var headerView: some View {
    HStack {
        Button(action: onBack) {
            Image(systemName: "chevron.left")
                .foregroundColor(.holoTextPrimary)
        }
        Spacer()
        Text("习惯")
            .font(.holoHeading)
        Spacer()
        Button {
            showAddHabit = true
        } label: {
            Image(systemName: "plus")
                .font(.system(size: 18, weight: .semibold))
                .foregroundColor(.white)
                .frame(width: 36, height: 36)
                .background(Color.holoPrimary)
                .clipShape(Circle())
        }
    }
    .padding(.horizontal, HoloSpacing.lg)
    .padding(.vertical, HoloSpacing.md)
}
```

- [ ] **Step 4: Create the settings list with visibility toggles and drag handles**

```swift
struct HabitStatsSettingsView: View {
    let onBack: () -> Void
    @StateObject private var repository = HabitRepository.shared
    @StateObject private var settings = HabitStatsDisplaySettings.shared

    var body: some View {
        NavigationStack {
            List {
                Section("统计页展示") {
                    ForEach(repository.activeHabits) { habit in
                        HStack {
                            Text(habit.name)
                            Spacer()
                            Toggle("", isOn: binding(for: habit.id))
                                .labelsHidden()
                        }
                    }
                    .onMove(perform: settings.moveHabit)
                }
            }
            .environment(\.editMode, .constant(.active))
            .navigationTitle("设置")
        }
    }

    private func binding(for habitId: UUID) -> Binding<Bool> {
        Binding(
            get: { settings.visibleHabitIds.isEmpty || settings.visibleHabitIds.contains(habitId) },
            set: { isVisible in
                var ids = settings.visibleHabitIds.isEmpty
                    ? repository.activeHabits.map(\.id)
                    : settings.visibleHabitIds
                if isVisible {
                    if !ids.contains(habitId) { ids.append(habitId) }
                } else {
                    ids.removeAll { $0 == habitId }
                }
                settings.setVisibleHabitIds(ids)
            }
        )
    }
}
```

- [ ] **Step 5: Build and manually verify the tab flow**

Run:

```bash
xcodebuild build \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
```

Expected: BUILD SUCCEEDED and the app opens with bottom tabs `统计 / 习惯 / 设置`, while the add button lives inside `习惯`.

- [ ] **Step 6: Commit**

```bash
git add \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/HabitsView.swift" \
  "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsSettingsView.swift"
git commit -m "feat(habits): add stats settings tab"
```

### Task 6: Polish Empty States, Regression Safety, and Manual QA

**Files:**
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsView.swift`
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Views/Habits/Stats/HabitStatsSettingsView.swift`
- Modify: `/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo/Models/HabitStatsState.swift`

- [ ] **Step 1: Add the empty states from the spec**

```swift
@ViewBuilder
private var emptyState: some View {
    if state.hasAnyHabits && state.displayItems.isEmpty {
        ContentUnavailableView(
            "当前统计页没有已启用的习惯",
            systemImage: "slider.horizontal.3",
            description: Text("去设置页选择要展示在统计页的习惯。")
        )
    } else if !state.hasAnyHabits {
        ContentUnavailableView(
            "还没有习惯",
            systemImage: "checkmark.circle",
            description: Text("先去习惯页创建你的第一个习惯。")
        )
    }
}
```

- [ ] **Step 2: Add the no-record fallback copy for empty weeks/months**

```swift
private var footerText: some View {
    if item.collapsedWeek.days.allSatisfy({ !$0.hasRecord }) {
        Text("本月暂无记录")
            .font(.holoLabel)
            .foregroundColor(.holoTextSecondary)
    }
}
```

- [ ] **Step 3: Run the full regression suite**

Run:

```bash
xcodebuild test \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
```

Expected: PASS for all tests in `HoloTests`.

- [ ] **Step 4: Run a manual QA pass on the simulator**

Run:

```bash
xcodebuild build \
  -project "/Users/tangyuxuan/Desktop/Claude/HOLO/Holo/Holo APP/Holo/Holo.xcodeproj" \
  -scheme "Holo" \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
open -a Simulator
```

Expected manual checklist:

- Bottom tabs show `统计 / 习惯 / 设置`
- Statistics page defaults to the selected month
- Default collapsed week uses the last recorded week in that month
- Only one card expands at a time
- Tapping the expanded card collapses it
- Settings toggles and reorder persist after relaunch
- Habit add flow still works from the `习惯` tab

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "fix(habits): polish stats dashboard states"
```

---

## Spec Coverage Check

- `轻量总览 + 周视图列表`: Task 3 and Task 4
- `当前自然月 + 点击切换月份`: Task 3 and Task 4
- `默认周视图，单开展开整月`: Task 1, Task 3, and Task 4
- `最后一个有记录的周`: Task 1
- `设置页只负责展示与排序`: Task 2 and Task 5
- `底部导航改为统计 / 习惯 / 设置`: Task 5
- `新增按钮下放到习惯 Tab`: Task 5
- `测量型只表达有记录，不做热力深浅`: Task 1 and Task 3
- `空态与无记录态`: Task 6

## Placeholder Scan

- No `TODO` / `TBD` / “similar to previous task” placeholders remain.
- Every task lists exact files, code snippets, commands, and expected outcomes.

## Type Consistency Check

- Shared types use one naming set throughout: `HabitStatsDisplayItem`, `HabitStatsWeekSlice`, `HabitStatsMonthSection`, `HabitStatsDisplaySettings`.
- State entry points stay consistent: `reload()`, `selectMonth(_:)`, `toggleExpansion(for:)`.
