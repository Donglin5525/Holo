services:
  holo-backend:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: holo-backend
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/data
